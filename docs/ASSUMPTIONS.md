# ASSUMPTIONS.md — KipLog BPS

Format: `[YYYY-MM-DD] Konteks → Keputusan → Alasan`

---

[2026-08-16] Repository ini belum berupa git repo dan tidak ada indikasi akan dipublikasikan segera → `performance-plans-2026.ts` (berisi `teamLeader` asli) dipakai apa adanya untuk seed lokal, TIDAK dibuatkan `.example.ts` dulu → PRD DR-08 hanya mewajibkan penyamaran itu **jika repo dipublikasikan**; ini tetap lokal. **Wajib direvisit sebelum `git init` + remote publik dibuat** — tandai TODO di README.

[2026-08-16] Nama repository untuk GitHub Pages (`vite.config.ts` base) belum dikonfirmasi pengguna → memakai `kiplog-bps` sesuai nama direktori contoh di PRD §8.2 → paling sederhana, mudah diubah sebelum deploy pertama (satu baris config), tidak menutup opsi lain.

[2026-08-16] Profil pengguna asli (nama, NIP, jabatan, unit) belum diberikan → tidak di-seed sama sekali; mengikuti FR-DAT-02, onboarding meminta pengguna mengisi sendiri saat pertama kali membuka aplikasi → PRD sendiri sudah mewajibkan alur ini, tidak ada yang perlu diputuskan.

[2026-08-16] Logo BPS dan logo Zona Integritas belum tersedia sebagai file → kop PDF §13.1 dirender dengan ruang kosong (sesuai instruksi eksplisit PRD: "jika tidak ada, sisakan ruang kosong tanpa merusak tata letak") → tidak menebak/membuat logo palsu; pengguna dapat mengunggah via My Profile (`logoDataUrl`) kapan saja.

[2026-08-16] Daftar hari libur nasional 2026 (SKB 3 Menteri) belum dikonfirmasi pengguna → `holidays-id.ts` diseed dengan daftar hari libur nasional 2026 yang umum diketahui (Tahun Baru, Imlek, Nyepi, Wafat Isa Almasih, Idul Fitri, dll — tanggal akan diverifikasi ulang) DAN halaman Pengaturan menyediakan editor agar pengguna mengoreksi sendiri → PRD §12.4 sudah mensyaratkan "daftar wajib diverifikasi pengguna setiap tahun"; ini bukan keputusan yang mengunci, hanya titik awal yang bisa diedit UI.

[2026-08-16] Zona waktu → memakai default `Asia/Makassar` (WITA) sesuai PRD §9.1, dapat diubah di Pengaturan → PRD sudah menetapkan ini sebagai default eksplisit, bukan tebakan baru.

[2026-08-16] `browser-image-compression` secara default menjalankan web worker yang memuat kode dari CDN (`cdn.jsdelivr.net`) via `importScripts` → berkas library disalin ke `public/vendor/browser-image-compression.js` dan dipakai lewat opsi `libURL` agar tetap same-origin → CDN itu jelas melanggar CON-06/SEC-02 (nol permintaan jaringan pihak ketiga); alternatif `useWebWorker:false` menghindari network tapi membebani main thread saat kompresi 20 gambar sekaligus (Fase 3 DoD), jadi self-host dipilih agar performa web worker tetap dipakai tanpa network eksternal.

[2026-08-16] CSP `object-src` diubah dari `'none'` ke `'self' blob:` → dibutuhkan agar preview PDF bukti dukung via `<embed>`/`<object>` terhadap Blob URL (FR-EVD-06) tidak diblokir → tetap ketat (hanya same-origin + blob, bukan domain eksternal), konsisten dengan tujuan SEC-08.

[2026-08-16] §12.1.2 menulis skor keyword sebagai `(jumlah cocok / jumlah keyword) × 40` → diimplementasikan sebagai fungsi tangga (0 cocok → 0, 1 cocok → 32, ≥2 cocok → 40) alih-alih pembagian literal terhadap total keyword → dihitung manual, formula literal menghasilkan skor ~11–15 untuk kasus uji wajib §12.1.5 (mis. RK #2 dari deskripsi SNLIK), jauh di bawah ambang 35, karena daftar keyword panjangnya bervariasi (3–8 item) sehingga pembagi besar menghukum RK dengan keyword lengkap. Keyword di seed ini adalah istilah spesifik/jarang (snlik, se2026, desa cantik) yang sengaja dikuratori — satu-dua kecocokan sudah sinyal kuat. Dikalibrasi ulang lalu **diverifikasi lulus** ke-5 kasus §12.1.5 (`tests/lib/matching/rk-matcher.test.ts`), bukan sekadar hitungan manual.

[2026-08-16] Deploy pertama (DEP-01, git push ke remote GitHub) TIDAK dijalankan otomatis meski termasuk DoD Fase 1 → workflow `.github/workflows/deploy.yml` disiapkan dan build/verify lokal dijalankan, tetapi `git init`/`git remote add`/`git push` menunggu konfirmasi eksplisit pengguna → push ke remote adalah aksi yang terlihat pihak lain dan sulit dibalik; sesuai kebijakan kerja, aksi semacam ini perlu persetujuan eksplisit per aksi, bukan tersirat dari "lanjutkan".

[2026-08-16] pdfmake/exceljs/jszip (§13, Fase 5) TIDAK diimpor statis di top-level → semuanya di-`import()` secara dinamis di dalam fungsi service (`lib/reporting/*.ts`, `lib/services/backup.ts`), sehingga Rollup memecahnya jadi chunk terpisah yang hanya diambil saat pengguna benar-benar membuat laporan/backup → NFR-02 secara eksplisit mensyaratkan "< 300 KB gzip (lazy-load pdfmake, exceljs, jszip)"; melakukan ini sejak Fase 5 (bukan menunda ke Fase 7) menjaga bundle awal tetap 251 KB gzip tanpa perlu pekerjaan tambahan nanti.

[2026-08-16] Logo Zona Integritas pada kop PDF §13.1 tidak punya sumber data — skema `UserProfileSchema` (dikunci sejak Fase 1) hanya punya satu slot `logoDataUrl` yang dipakai untuk logo kiri/unit → kotak logo ZI di kop PDF selalu dikosongkan (ruang kosong, bukan logo palsu), konsisten dengan aturan "jika tidak ada, sisakan ruang kosong" → menambah field kedua berarti mengubah skema inti di luar lingkup Fase 5; tidak ada AC yang mensyaratkan dua slot logo terisi.

[2026-08-16] Penomoran folder RK pada Evidence Pack ZIP (§13.3, format `RK-02-...`) tidak dijelaskan sumbernya secara eksplisit di PRD → dipakai posisi 1-indeks RK dalam `performancePlans` terurut `sortOrder` (urutan seed asli) → ini konsisten dengan penomoran RK yang sudah dipakai di tempat lain (mis. wireframe "RK 2 dari 9" §14.6), dan `sortOrder` adalah satu-satunya urutan RK yang stabil di data model.

[2026-08-16] Backup JSON ringan (FR-DAT-07) tidak menyertakan `Evidence.blob`/`thumbnailBlob` (Blob tidak bisa di-`JSON.stringify`) → field itu dihilangkan saat ekspor, direstorasi kosong saat impor JSON; hanya Backup Lengkap (ZIP, FR-DAT-08) yang menyimpan berkas asli di folder `evidence/` dan mengembalikannya saat impor → PRD sendiri membedakan dua mode backup ini ("Lengkap" vs biasa), jadi kehilangan berkas pada mode ringan adalah trade-off yang disengaja, bukan bug — diverifikasi lewat round-trip nyata (hapus semua data → impor ZIP → blob bukti dukung utuh kembali).

[2026-08-16] Mode Impor "Gabungkan" (FR-DAT-09) tidak menyentuh `UserProfile`/`AppSettings` sama sekali (hanya mode "Ganti" yang menulis keduanya) → karena keduanya singleton (id tetap `'me'`/`'settings'`), konsep "lewati ID duplikat" tidak relevan untuk record tunggal — menimpa profil/pengaturan perangkat saat ini secara diam-diam saat "menggabungkan" data lain akan mengejutkan pengguna; profil/pengaturan lokal yang aktif tetap dipertahankan pada mode Gabungkan.

[2026-08-16] Bug ditemukan saat pengujian browser langsung: gambar bukti dukung yang korup/tidak dapat didekode membuat `pdfMake.createPdf().getBlob()` macet tanpa batas (callback tidak pernah dipanggil, tidak ada error yang bisa ditangkap) → `lib/reporting/pdf-data-dukung.ts` sekarang memvalidasi setiap gambar lewat `createImageBitmap()` sebelum diserahkan ke pdfmake; gambar yang gagal didekode jatuh ke rujukan teks biasa ("gambar tidak dapat dibaca") alih-alih menghentikan seluruh pembuatan laporan.

[2026-08-16] Bug ditemukan saat pengujian browser langsung: urutan bukti dukung pada PDF/Excel/ZIP tidak mengikuti `Evidence.sortOrder` yang diatur pengguna, karena `LaporanPage`/`BuktiTautanPage` mengelompokkan hasil `evidenceRepository.list()` (urutan primary-key Dexie, efektif acak untuk UUID) tanpa mengurutkan ulang tiap kelompok → dibuat helper bersama `lib/services/evidence-grouping.ts` yang selalu mengurutkan tiap grup berdasarkan `sortOrder`; diverifikasi lewat regenerasi PDF nyata yang sekarang menampilkan bukti secara konsisten sesuai urutan yang disimpan.

[2026-08-16] Copy Mode KipApp Ready (§14.6) memperlihatkan setiap kegiatan dalam satu RK sebagai kartu tersendiri yang ditumpuk (bukan satu kartu per RK dengan navigasi kegiatan tersembunyi) → wireframe PRD hanya menampilkan satu kartu sebagai contoh ilustratif, tetapi frasa "daftar kegiatan dikelompokkan per RK" (FR-KAR-02) menyiratkan daftar, bukan satu item pada satu waktu → dipilih agar seluruh kegiatan dalam RK tetap terlihat dan dapat disalin tanpa berpindah state; navigasi J/K (FR-KAR-10) tetap dipertahankan untuk memindahkan fokus antar kartu.

[2026-08-16] recharts (grafik distribusi RK di Dashboard, FR-DSH-07) di-lazy-load via `React.lazy` alih-alih diimpor statis → recharts menambah ~101 KB gzip ke bundle utama saat diimpor langsung dari `DashboardPage` (rute `/`), mendorong bundle awal dari 251 KB ke 362 KB gzip dan melanggar NFR-02 (< 300 KB) → dipisah jadi chunk async sendiri (dimuat paralel saat Dashboard dirender, bukan diblokir sebelum render pertama), bundle utama kembali ke 259 KB gzip. Grafik adalah fitur P1, bukan kritis untuk render pertama.

[2026-08-16] Bug ditemukan saat pengujian browser langsung: kartu statistik "Coverage" di Dashboard dan Monthly Review menampilkan rasio yang secara matematis tidak konsisten dengan persentase di sampingnya (mis. "60% ... 6/20 hari kerja" — 6/20 sebenarnya 30%) → penyebabnya adalah pembilang (`filledWorkdays`) dihitung dari hari kerja yang dibatasi hingga hari ini, sedangkan penyebut yang ditampilkan (`workdaysInMonth`) memakai total hari kerja satu bulan penuh (tidak dibatasi) → diperbaiki dengan memisahkan `workdaysUpToToday`/`workdaysCounted` sebagai penyebut yang benar untuk label Coverage, dan menambah kartu "Hari Kerja" (total bulan penuh) sebagai stat terpisah agar FR-DSH-02 tetap menampilkan hari kerja/terisi/kosong sebagai tiga angka berbeda yang masing-masing benar.

[2026-08-16] Audit UI/UX Fase 7 (ui-ux-pro-max) menemukan `public/favicon.svg` dan `public/icons.svg` adalah aset template yang tidak berkaitan (logo ungu + ikon sosial Discord/GitHub/X, bukan identitas KipLog) — `icons.svg` bahkan hanya berisi elemen `<symbol>` tak-terpakai yang tidak merender apa pun sebagai ikon manifest PWA. Diganti dengan mark KipLog sendiri (clipboard + centang, biru primer) yang divalidasi berada dalam safe-zone maskable 80% dan dirender ulang lewat kanvas untuk konfirmasi visual.

[2026-08-16] Audit yang sama menemukan token `--warning`/`--success` di §14.2 (`#d97706`/`#16a34a` persis) gagal WCAG AA 4.5:1 sebagai warna teks di atas latar putih (terukur 3.16:1 dan 3.91:1) — sementara NFR-09 (WCAG 2.1 AA) juga wajib dipenuhi dalam PRD yang sama → nilai lightness kedua token digelapkan secukupnya (34%/30%) agar lolos 4.5:1, sambil tetap dikenali sebagai oranye/hijau yang sama; dipakai konsisten untuk teks maupun badge/border/icon (yang sebenarnya hanya perlu 3:1) demi kesederhanaan. Mode gelap tidak diubah — sudah lolos 7:1+.

[2026-08-16] §14.8 mensyaratkan ukuran target sentuh ≥44×44px dan sidebar "menciut jadi ikon" pada 768–1279px — keduanya belum diimplementasikan (tombol shadcn default 36-40px; sidebar selalu lebar penuh). Diperbaiki: `Button`/`Input` dinaikkan ke ≥44px, `Sidebar` menciut ke ikon-saja (dengan `title` sebagai tooltip native) antara `md` dan `xl`, dan Dialog aktivitas kini menjadi bottom sheet penuh-lebar di bawah `md` (sebelumnya selalu modal tengah) sesuai "form jadi bottom sheet". Toolbar padat `EvidenceCard` (6 tombol ikon per kartu) dinaikkan sebagian (24px→32px) — 44px penuh akan meluap dari lebar kartu; redesain penuh ke menu overflow "⋯" di-spawn sebagai tugas terpisah, bukan dikerjakan di sini.

[2026-08-16] §14.8 juga mensyaratkan "kalender jadi agenda" di bawah 768px, tetapi `KalenderPage` hanya menyusutkan grid 7-kolom `MonthView` tanpa overflow (bukan benar-benar berganti ke daftar) → dibuat `AgendaView` (daftar hari yang dapat digulir, satu baris per tanggal) yang ditampilkan lewat CSS (`md:hidden`/`hidden md:block`) berdampingan dengan `MonthView`, tanpa listener resize JS.

[2026-08-16] Audit pesan error §17.1 Fase 7 menemukan beberapa teks yang tidak persis sama dengan spesifikasi (atau hilang sama sekali): pesan gagal unggah bukti dukung kehilangan kalimat "File asli masih ada di perangkat Anda. Coba lagi."; pesan URL tidak valid punya 4 varian berbeda di 4 tempat (kini disatukan jadi "Link bukti dukung harus diawali https:// atau http://." persis); pesan periode terkunci hilang kalimat kedua tentang cara membuka kunci (dan `ActivityFormModal` salah selalu menampilkan pesan "sudah dikirim untuk dinilai" walau penyebab sebenarnya periode terkunci, bukan `sentForReview` — kini dibedakan); pesan backup versi lebih baru memakai kalimat berbeda dari spesifikasi (kini disamakan persis, angka versi skema dijatuhkan karena PRD tidak memintanya). Juga ditemukan **tidak ada** penanganan untuk dua skenario wajib §17.1: kegagalan generate PDF (sekarang dibungkus try/catch dengan pesan baku, ditampilkan lewat `ErrorBanner` baru di `LaporanPage`/`BuktiTautanPage`) dan IndexedDB yang diblokir browser (kini dicek di `main.tsx` sebelum render lewat `checkIndexedDbAvailable()`, menampilkan pesan baku alih-alih membiarkan Dexie gagal dengan cara yang membingungkan).

[2026-08-16] CRUD penuh untuk Rencana Kinerja serta Import RK dari CSV/JSON (FR-DAT-03/04/05, berstatus P0 di PRD) **tidak pernah masuk ke daftar kerja fase mana pun** di §20 — Fase 1 hanya menyebut "seeding 40 RK 2026", dan tidak ada fase lain yang menyebut CRUD/import RK secara eksplisit (berbeda dari FR-DAT-06 yang eksplisit disebut di Fase 7). `RencanaKinerjaPage` karena itu tetap read-only sejak Fase 1, dengan komentar kode yang (keliru) mengklaim keputusan ini "dicatat di ASSUMPTIONS.md" — padahal belum pernah benar-benar ditulis di sini sampai sekarang. Diperbaiki sebagian saat audit empty state Fase 7: tombol "Muat 40 RK 2026" pada empty state sekarang benar-benar berfungsi (memanggil `seedPerformancePlansIfEmpty()`); tombol "Import CSV" tetap belum ada — fitur CSV import (parsing, validasi per baris sesuai §10.16.1, pratinjau, transaksi all-or-nothing) di-spawn sebagai tugas latar terpisah karena ukurannya sebanding dengan fitur satu-fase penuh, bukan sekadar polish.

[2026-08-16] Audit PWA (task #63) menemukan dua bug nyata di konfigurasi `vite-plugin-pwa`: (1) `workbox.globPatterns` tidak menyertakan `woff2`, sehingga font Inter/JetBrains Mono self-hosted yang baru ditambahkan (§14.2) tidak ikut di-precache — offline reload akan jatuh ke font fallback, melanggar PWA-03 "berfungsi penuh secara offline". Diperbaiki dengan menambah `woff2` ke `globPatterns`. (2) Saat memperbaiki ini, ditemukan bahwa import `@fontsource/*` default (`inter/400.css`, dst.) menyertakan SEMUA subset unicode (latin, latin-ext, cyrillic, greek, greek-ext, vietnamese) meski KipLog hanya berbahasa Indonesia (latin+latin-ext) — total 74 file font ikut di-build dan akan ikut di-precache, membengkakkan cache offline secara signifikan tanpa manfaat. Diperbaiki dengan mengganti ke import per-subset (`@fontsource/inter/latin-400.css`, `.../latin-ext-400.css`, dst.) di `src/styles/theme.css`, memangkas jadi 24 file. Precache akhir: 28 entries (~4.7 MB) — didominasi `pdfmake`/`exceljs`/`vfs_fonts` (chunk >500KB, sudah diberi peringatan build sejak Fase 5, di luar cakupan audit PWA ini).

[2026-08-16] **Keterbatasan lingkungan pengujian**: `navigator.serviceWorker.register()` gagal dengan "An unknown error occurred when fetching the script" di dalam Browser pane (Electron/Claude Desktop embedded browser) — dikonfirmasi ini BUKAN bug KipLog karena kegagalan identik terjadi saat mencoba me-register service worker sample resmi Google (`googlechrome.github.io/samples/service-worker/basic/`) di origin yang sama sekali tidak berkaitan. Alat browser Chrome asli (`claude-in-chrome`) juga tidak dapat menjangkau `localhost` (sandbox jaringan terpisah). Karena itu, registrasi SW aktual, caching offline runtime, dan alur "Versi baru tersedia. Muat ulang?" **tidak dapat diverifikasi end-to-end secara langsung di lingkungan ini** — verifikasi dilakukan secara statis: konfigurasi `vite-plugin-pwa` (manifest, `registerType: 'prompt'`, `globPatterns`), isi `dist/sw.js` hasil build (precache manifest 28 entries mencakup seluruh app shell + font), dan kode `PwaUpdatePrompt`/`useRegisterSW` ditinjau langsung. Verifikasi manual oleh pengguna di Chrome/Edge desktop asli (DevTools → Application → Service Workers, lalu uji offline) direkomendasikan sebelum deploy produksi.


[2026-08-16] FR-DAT-06 (Import Excel Pelaksanaan dari KipApp) diimplementasikan sebagai kartu baru di halaman Backup & Import (bukan halaman/rute baru -- SS14.3 mengunci daftar navigasi, dan SS11.1/Deliverable-0 tidak menyebut rute baru untuk fitur ini). Sesuai teks FR-DAT-06 ("tandai kegiatan yang sudah ada... tampilkan kegiatan yang ada di KipApp tetapi belum ada di KipLog"), fitur ini murni rekonsiliasi tampilan -- tidak membuat/mengubah data apa pun, sehingga tidak memerlukan alur konfirmasi/undo seperti import backup. Pencocokan dilakukan per (tanggal, substring uraian kegiatan case-insensitive) karena KipApp dan KipLog tidak berbagi ID. Pemetaan kolom (SS17.1 "Struktur kolom tidak dikenali...") dicoba otomatis dengan menebak header (tanggal/date, uraian/kegiatan/deskripsi); jika gagal, pesan baku ditampilkan dan pengguna memetakan manual lewat dropdown -- diverifikasi kedua jalur (auto-detect berhasil dan gagal) langsung di browser.


[2026-08-16] Audit performa Fase 7 (task #65) menemukan tiga bottleneck nyata yang mendorong skor Lighthouse mobile-throttled di bawah ambang: (1) seluruh 12 halaman diimpor statis di `routes.tsx`, sehingga membuka Dashboard memuat kode Kalender/Kegiatan/Laporan/dst sekaligus -> dipecah pakai React.lazy per rute + Suspense, chunk index utama turun dari 262 KB menjadi 233 KB gzip. (2) `main.tsx` memblokir root.render() PERTAMA di belakang probe IndexedDB async (`checkIndexedDbAvailable`, timeout 2 detik) yang ditambahkan saat audit pesan error (task #62) -> diubah agar App di-render langsung secara sinkron, probe berjalan paralel dan hanya menukar ke `StorageBlockedMessage` jika hasilnya negatif; ini memperbaiki First Contentful Paint tanpa mengorbankan proteksi §17.1 (RootErrorBoundary tetap menangkap error Dexie transien pada jendela singkat sebelum swap). (3) `AppShell` (dimuat di setiap rute) mengimpor `ActivityFormModal` secara statis, menyeret react-hook-form/zod/RK-matcher/fuzzy-search ke bundle awal meski modal belum tentu dibuka -> diubah jadi `React.lazy`, hanya dimuat saat `isOpen` true; chunk index utama turun lagi ke 166 KB gzip (dari 262 KB semula). Juga ditemukan `CardTitle` selalu me-render `<h3>` padahal setiap halaman sudah punya `<h1>` dari `PageHeader` tanpa `<h2>` di antaranya (pelanggaran urutan heading NFR-09/heading-order) -> diubah ke `<h2>`, skor Accessibility Lighthouse naik dari 96 ke 100. Dependency `date-fns` yang tidak pernah diimpor di mana pun dihapus dari `package.json` (bukan tercount di bundle karena tidak diimpor, tapi tetap dead weight).

Hasil Lighthouse akhir (dijalankan bersih, tanpa proses Node lain yang bersaing untuk CPU -- percobaan awal sempat menunjukkan Total Blocking Time palsu 1000ms+ akibat beberapa server preview/build berjalan bersamaan, dikonfirmasi ulang dengan lingkungan bersih): mode mobile default (throttling CPU 4x + jaringan lambat) -> Performance 86-87, Accessibility 100, Best Practices 100 (dua kali percobaan bersih untuk cek stabilitas). Mode `--preset=desktop` (tanpa throttling) -> Performance 99, Accessibility 100, Best Practices 100. NFR-10 ("Lighthouse >= 90") **terpenuhi untuk Accessibility dan Best Practices di kedua mode, dan untuk Performance di mode desktop, tapi TIDAK terpenuhi untuk Performance di mode mobile default** (86-87 vs ambang 90). Sisa gap sepenuhnya berasal dari First Contentful Paint (skor 0.45) dan Largest Contentful Paint (skor 0.72) -- Total Blocking Time sudah sempurna (40ms, skor 1.0) setelah perbaikan di atas. Ini karakteristik struktural aplikasi client-side-rendered (shell HTML kosong sampai bundle JS diunduh+dieksekusi) yang throttle mobile default Lighthouse (4x CPU + jaringan ~1.6 Mbps) sangat menghukum; memperbaikinya lebih jauh butuh SSR/prerendering atau vendor-chunk-splitting mendalam (react-hook-form/zod/dexie dipakai luas lewat barrel export `src/types/index.ts`) -- di luar cakupan wajar polish Fase 7 pada tahap akhir implementasi, dan berisiko mengganggu stabilitas 99 test yang sudah lulus. Dicatat sebagai gap yang diketahui, bukan diklaim terpenuhi.

NFR-01 s.d. NFR-11 -- ringkasan status verifikasi Fase 7: NFR-01 (muat awal <2 detik) tidak diukur terpisah dari FCP/LCP Lighthouse di atas (~3.1s throttled mobile, jauh di bawah 2 detik pada kondisi nyata/desktop). NFR-02 (bundle JS awal <300 KB gzip) TERPENUHI (166 KB gzip untuk chunk index utama setelah optimasi di atas). NFR-03/04/05 (render kalender, global search, simpan kegiatan) sudah diverifikasi lewat pengujian manual di fase-fase sebelumnya (Fase 2-4), tidak diulang di Fase 7. NFR-06 (generate PDF 30 kegiatan) sudah diverifikasi di Fase 5. NFR-07 (skala data 5000/15000/500) belum diuji langsung dengan volume sebesar itu -- hanya diuji dengan data seed kecil; ini bukan pengujian beban formal. NFR-08 (dukungan browser Chrome/Edge/Firefox/Safari versi minimum) adalah klaim kompatibilitas berbasis fitur web standar yang dipakai (IndexedDB, Service Worker, dll), tidak diuji lintas-browser nyata di lingkungan ini. NFR-09 (WCAG 2.1 AA) diverifikasi lewat audit kontras manual (Fase 7 awal) + Lighthouse Accessibility 100. NFR-10 lihat paragraf di atas (terpenuhi sebagian). NFR-11 (nol permintaan pihak ketiga) TERPENUHI -- diverifikasi lewat DevTools Network di browser, seluruh permintaan same-origin.


[2026-08-16] Regresi nyata ditemukan saat pengujian browser langsung pasca-optimasi performa (task #67): (1) Membuka `ActivityFormModal` lazy-loaded lewat klik langsung memicu peringatan React "A component suspended while responding to synchronous input" dan berpotensi memicu reset error-boundary di tengah interaksi -- diperbaiki dengan mount `ActivityFormModal` tanpa syarat di balik `Suspense` sejak `AppShell` pertama kali render (bukan digerbang oleh `isOpen`), sehingga chunk-nya mulai diunduh begitu shell termuat, bukan saat tombol diklik; FCP tetap tidak terbebani karena unduhan tetap async di luar bundle utama. (2) `checkIndexedDbAvailable()` (§17.1, ditambahkan saat audit pesan error) memakai database percobaan sekali-pakai (`__kiplog_probe__`) yang perlu transaksi `onupgradeneeded` karena belum pernah ada -- ditemukan lewat pengujian tab baru bahwa `onsuccess` TIDAK PERNAH terpanggil setelah `onupgradeneeded` di lingkungan pengujian ini (dikonfirmasi: `indexedDB.open('kiplog-bps')` yang sudah ada berhasil dalam 1ms, tapi `indexedDB.open('__kiplog_probe__')` yang baru macet total), membuat probe gagal-aman ke pesan "Browser ini memblokir penyimpanan lokal" secara KELIRU meski penyimpanan sebenarnya berfungsi normal. Diperbaiki dengan membuka nama database aplikasi asli (`kiplog-bps`, sama dengan `src/db/database.ts`) tanpa versi eksplisit alih-alih nama sekali-pakai -- untuk kasus umum (database sudah ada, yaitu hampir seluruh pemakaian setelah peluncuran pertama) probe sekarang berhasil cepat; untuk pengguna baru pertama kali di lingkungan yang benar-benar bermasalah seperti ini, probe akan tetap gagal, tapi itu benar karena Dexie sendiri juga akan macet membuka database yang sama -- perilaku gate jadi konsisten dengan kondisi nyata aplikasi, bukan false positive dari nama database terpisah. Kedua perbaikan diverifikasi lewat tab browser baru (bukan HMR reload) tanpa error konsol dan modal terbuka penuh berisi form.


[2026-08-16] Sebelum push pertama ke github.com/FlameHeater/kiplog-bps (repo publik, dikonfirmasi pengguna), nama Ketua Tim asli di `src/data/performance-plans-2026.ts` (map `L`) diganti label generik "Ketua Tim 1".."Ketua Tim 12" -- key tetap sama sehingga tidak ada referensi lain di kodebase yang perlu diubah, dan pengelompokan per Ketua Tim (fitur P1 Fase 6) tetap berfungsi karena nilainya tetap unik per orang. Ini menyelesaikan TODO privasi DR-08 yang dicatat sejak entri ASSUMPTIONS.md pertama (2026-08-16, baris pertama file ini).


[2026-08-16] Pengguna menilai UI/UX terlalu "biasa" pasca-deploy pertama dan meminta poles cepat -> alih-alih mengganti palet warna (PRD SS14.2 mengunci biru #2563eb, sudah dipakai di ikon PWA/favicon yang sudah di-commit), dilakukan poles kedalaman visual pada token yang sama: latar halaman diberi warna sedikit ke-abu-abuan (--background: 210 25% 97% alih-alih putih murni) supaya Card putih tampak "mengambang", skala shadow baru (elevation-1/2/glow) diterapkan ke Card dan StatTile, Button diberi gradient + shadow + feedback tekan (active:scale-97), Sidebar diberi logo gradient dan indikator aktif garis kiri, dan header Dashboard diberi wash gradient lembut. Ditambahkan juga guard global prefers-reduced-motion (belum ada sebelumnya) karena transisi baru ditambahkan. Bug ditemukan saat verifikasi: nama key boxShadow "card" bentrok dengan nama token warna "card" di config Tailwind yang sama -> Tailwind diam-diam mengganti warna shadow literal dengan hsl(var(--card)) (putih), membuat shadow tak terlihat -> diganti nama jadi "elevation-1"/"elevation-2"/"elevation-glow" untuk hindari tabrakan nama; diverifikasi lewat getComputedStyle di browser bahwa --tw-shadow sekarang benar-benar memakai rgba yang dimaksud, bukan hsl(var(--card)).


[2026-08-17] Bug serius ditemukan saat pengujian browser langsung fitur import PDF: membuka "Ubah" pada kegiatan yang sudah ada kadang menampilkan form KOSONG (tanggal hari ini, jam default, deskripsi kosong) padahal judul dialog tetap benar "Ubah Kegiatan" -- terlihat identik dengan form "Tambah Kegiatan" baru, sehingga pengguna bisa tanpa sadar menyimpan data kosong menimpa kegiatan asli. Akar masalah: `useActivity` (live query Dexie) tidak langsung mereset ke `undefined` saat `editingId` berubah -- menutup modal membawanya lewat `id=null` (existing=null), dan membuka ulang kegiatan yang SAMA sempat menampilkan `null` basi itu sebelum query baru selesai, padahal `editingId` sudah kembali ke id asli. Guard loading lama (`existing === undefined`) tidak menangkap kondisi ini karena `null` bukan `undefined` -- `loading` jadi `false` satu render terlalu cepat, dan `ActivityForm` ter-mount dengan `existing=null` (mode "kegiatan baru"). Diperbaiki di `ActivityFormModal.tsx`: `loading` sekarang juga true selama `existing` belum benar-benar cocok dengan `editingId` (null ATAU id tidak cocok), bukan hanya saat `undefined`. Ditambahkan dua lapis pertahanan lain: (1) `ActivityForm` diberi `key={editingId}` di `ActivityFormModal` agar setiap kegiatan selalu dapat mount baru; (2) `ActivityForm` memanggil `reset(computedDefaults)` lewat `useEffect` sekali saat mount sebagai jaring pengaman tambahan terhadap react-hook-form's `defaultValues` yang gagal ter-terap ke DOM pada skenario async/Suspense tertentu. **Autosave juga diperbaiki terpisah**: guard lama (`isUntouched = !existing && description kosong`) hanya melindungi kegiatan BARU yang belum disentuh -- kegiatan yang SEDANG diedit selalu dianggap "tersentuh" apa pun isinya, jadi kalau form pernah ter-render dengan data salah (skenario di atas), autosave 2 detik kemudian akan MENULIS data kosong itu ke database, menimpa data asli secara permanen. Diganti pakai `formState.isDirty` dari react-hook-form (true hanya kalau pengguna benar-benar mengubah sesuatu) -- kombinasi ketiga perbaikan ini memastikan baik tampilan maupun penyimpanan tidak pernah memakai data yang salah. Diverifikasi lewat: (a) tes regresi baru `ActivityFormModal.test.tsx` yang dikonfirmasi GAGAL tanpa perbaikan loading-gate dan LULUS dengannya; (b) uji manual berulang (6x buka-tutup-buka cepat lintas kegiatan berbeda) langsung di browser, semua menampilkan data benar; (c) dicek tidak ada data baru yang rusak di IndexedDB setelah stress test.


[2026-08-17] Pengguna memberikan berkas Word resmi kantor (Template Data Dukung Laporan Kipapp.docx) dan meminta PDF KipLog memakai tampilan yang sama. Berkas itu TIDAK punya bagian "footer" Word sungguhan (dicek: hanya ada relasi header, tidak ada footer.xml) -- kemungkinan istilah "footer" yang dimaksud pengguna adalah blok kop/checklist Reformasi Birokrasi yang berulang di setiap halaman, atau gaya tabel field-nya. Dibandingkan langsung (dirender ke PDF lewat Word COM) dengan PDF yang dihasilkan `generateDataDukungPdf`: struktur field (Tanggal/Waktu Kegiatan/Kegiatan/Capaian/Progress/Rencana Kinerja/Pelaksana/NIP) dan kop (logo kiri, 8 checkbox area RB dua kolom, logo RB kanan) sudah cocok persis -- perbedaan nyata yang ditemukan: tabel field template punya garis horizontal di bawah tiap baris (gaya Word "Table Grid" dengan garis vertikal dimatikan), sedangkan `buildFieldTable` sebelumnya memakai `noBorders` polos. Diperbaiki dengan custom pdfmake `layout` (hLineWidth 0.75 hitam di bawah tiap baris + penutup di bawah NIP, vLineWidth 0) agar PDF KipLog sekarang identik dengan template resmi ini. Diverifikasi dengan mengunduh PDF nyata dari aplikasi dan membandingkan render-nya langsung terhadap render Word dari template asli.


[2026-08-17] Pengguna klarifikasi: "footer" yang dimaksud sebelumnya sebenarnya adalah HEADER -- logo pada kop belum muncul di PDF KipLog. Dibongkar berkas Word resmi (image1.emf di dalamnya): ternyata satu gambar EMF tunggal berisi KEDUA logo (BPS kiri + Reformasi Birokrasi kanan) sekaligus checklist area RB yang sudah di-flatten jadi gambar statis -- bukan tiga elemen terpisah seperti dugaan awal. Karena checkbox versi KipLog yang hidup (mencerminkan rbAreas kegiatan yang sebenarnya, tercentang/tidak per kegiatan) lebih berguna daripada versi statis di gambar, hanya KEDUA LOGO yang di-crop dari gambar itu (via System.Drawing di PowerShell, karena tidak ada ImageMagick/ghostscript di lingkungan ini) dan disimpan sebagai `src/assets/logo-bps.png` + `src/assets/logo-rb.png`, lalu dipasang di `buildKop()`: logo BPS jadi DEFAULT saat `profile.logoDataUrl` kosong (sebelumnya selalu kosong kalau user belum unggah logo unit sendiri -- unggahan pengguna tetap diprioritaskan jika ada, untuk unit BPS Kabupaten/Kota yang mungkin punya varian logo sendiri), dan logo RB SELALU dipakai (sebelumnya permanen kosong karena skema tidak punya slot data untuk logo itu -- sekarang ada sumber resmi, bukan rekaan, jadi aturan "jangan bikin logo palsu" tidak dilanggar). Diverifikasi dengan mengunduh PDF sungguhan dari aplikasi -- sempat tampak gagal (kotak logo kosong) karena modul `pdf-data-dukung.ts` belum ter-hot-reload di tab yang sedang dites (dikonfirmasi lewat pengujian pdfmake terisolasi yang berhasil, lalu reload penuh membuktikan kode sudah benar); setelah reload, kedua logo tampil persis seperti template resmi.

[2026-08-17] Pengguna meminta lima perbaikan sekaligus pasca-deploy: (1) kartu Kegiatan tidak menampilkan tanggal, hanya rentang jam -> `ActivityCard` sekarang selalu menampilkan tanggal (format Indonesia) di depan, jam+durasi hanya ditampilkan kalau ada. (2) tidak ada cara melewati input jam saat mencatat kegiatan -> jam kini opsional: `startTime`/`endTime` boleh `''` ("waktu tidak dicatat", bukan `null`, supaya tipe tetap `string` di ~15 pemakai yang sudah ada) lewat `OptionalTimeStringSchema` di `activity.schema.ts`; checkbox "Tidak mencatat jam kegiatan" di `ActivityForm` menyembunyikan kedua input jam saat dicentang; validator "Ready to Report" memperlakukan KEDUA jam kosong sebagai state lengkap (opt-out sah), tapi HANYA SATU jam kosong tetap gagal (setengah-isi bukan pilihan sah); PDF Data Dukung dan Copy Mode menampilkan "-" / "jam tidak dicatat" saat kosong alih-alih string jam kosong yang aneh. (3) Import PDF aktivitas sebelumnya memakai jam kerja default (08:00-16:00) untuk semua draft -> dihapus total, `draftToActivity` sekarang selalu set `startTime`/`endTime` ke `''` karena timestamp per-baris di PDF sumber adalah waktu LOG (kapan baris dicatat sistem GPS), bukan waktu aktivitas sebenarnya -- mengklaim jam kerja default sebagai jam aktivitas nyata memberi kesan presisi yang tidak dimiliki data sumber; pengguna mengisi jam manual sendiri kalau perlu. (4) filter periode di halaman-halaman dengan tanggal dirasa sulit dinavigasi (`buildReportPeriod`'s cabang 'bulanan' selalu terkunci ke bulan berjalan, tidak bisa pilih bulan lain tanpa pindah ke mode 'Kustom') -> ditambah parameter opsional `monthAnchor` ("YYYY-MM") ke `buildReportPeriod`, dipakai lewat `<input type="month">` baru di `LaporanPage` (muncul di samping pilihan Periode saat "Bulanan" dipilih) dan di `KegiatanPage` (kontrol bulan cepat berdiri sendiri di samping tombol Filter yang langsung men-set dateFrom/dateTo lewat `monthRangeContaining`, tanpa perlu membuka popover filter). (5) kata kunci pencocokan RK otomatis (`rk-matcher.ts`) sebelumnya hanya bisa tumbuh lewat pembelajaran implisit (`applyLocalLearning` saat RK dipilih) -> ditambah `KeywordEditor` di `RencanaKinerjaPage`, komponen kecil per-RK dengan chip kata kunci + tombol hapus + input tambah, langsung menulis ke `performancePlanRepository.upsert`. Kelimanya diverifikasi lewat browser langsung (tanggal tampil di kartu, checkbox menyembunyikan input jam, filter bulan Kegiatan memfilter 208->62 kegiatan untuk Mei 2026, filter bulan Laporan menampilkan "01-31 Agustus 2026" default, kata kunci baru tersimpan dan bisa dihapus lagi) serta suite penuh (`typecheck`, `lint`, 104 test, `build` -- semua bersih).

[2026-08-17] Pengguna meminta pembaruan visual besar: UI dirasa "kuno", tidak ada gambar/ilustrasi sama sekali, dan dark mode belum berfungsi (variabel CSS `.dark` sudah ada sejak awal tapi tidak pernah ada logika yang menerapkannya ke DOM, dan tidak ada toggle-nya). Diklarifikasi dengan pengguna: "backend custom tampilan" yang dimaksud adalah panel pengaturan tampilan di dalam aplikasi (bukan server sungguhan -- aplikasi ini tetap 100% client-side/offline-first by design), dan pengguna memilih palet dasar Navy + Biru muda serta meminta pilihan warna aksen yang bisa diganti sendiri.
- **Riset gaya** (skill ui-ux-pro-max + frontend-design): merekomendasikan gaya "Soft UI Evolution" (radius 8-12px, shadow lembut berlapis) dikombinasi "Data-Dense Dashboard" (grid rapat, KPI row) -- KipLog sudah punya sebagian besar ini dari polesan sebelumnya (`elevation-1/2/glow`, radius kartu 10px, token `bg-gradient-primary`), jadi ini penghalusan bukan rombak total. Font diganti dari Inter-untuk-semua menjadi Lexend (heading) + Source Sans 3 (body) -- keduanya direkomendasikan khusus untuk produk pemerintah/aksesibilitas.
- **Palet warna**: `:root`/`.dark` di `theme.css` diperbarui ke palet Navy (`--background:#f8fafc`, `--foreground:#020617`, `--primary` navy `#0f172a` di mode terang). Sistem warna aksen ditambahkan lewat atribut `data-accent` di `<html>` (bukan custom color picker bebas -- dipilih kurasi 5 warna: Navy/Teal/Indigo/Emerald/Oranye, semuanya di shade ~700 untuk mode terang supaya teks putih di atasnya tetap lolos AA 4.5:1, mengikuti pola yang sama dipakai untuk token success/warning sebelumnya) agar tidak ada kombinasi yang tidak terbaca. `bg-gradient-primary`/`bg-gradient-subtle`/`shadow-elevation-glow` di `tailwind.config.js` diubah dari warna biru literal menjadi turunan `hsl(var(--primary))`, supaya otomatis ikut berubah sesuai aksen + mode gelap/terang tanpa perlu blok terpisah per aksen.
- **Bug ditemukan saat verifikasi browser**: `fontFamily.sans: ['Source Sans 3', ...]` di `tailwind.config.js` menghasilkan CSS `font-family: Source Sans 3, ...` TANPA tanda kutip -- karena "3" bukan awalan identifier CSS yang valid, browser gagal mem-parsing seluruh nilai font-family dan jatuh ke default serif (Times New Roman), padahal aturan CSS-nya sendiri terlihat benar. "JetBrains Mono" tidak kena masalah ini karena tidak ada digit di awal kata. Diperbaiki dengan mengutip manual: `'"Source Sans 3"'`. Ditemukan lewat `getComputedStyle(document.body).fontFamily` di browser, bukan lewat typecheck/lint/test (yang semuanya tidak mendeteksi masalah string CSS ini).
- **Dark mode**: hook baru `useAppliedTheme` (dipanggil sekali di `App.tsx`) membaca `settings.theme`/`settings.accentColor`, meresolusi `'system'` lewat `matchMedia`, lalu men-toggle class `dark` + `data-accent` di `<html>`. Toggle cepat (ikon matahari/bulan) ditambahkan di topbar (`AppShell.tsx`) untuk ganti terang<->gelap tanpa buka Pengaturan; kontrol lengkap Terang/Gelap/Otomatis + 5 swatch aksen ada di kartu baru "Tampilan" (`ThemeForm.tsx`) di halaman Pengaturan. Field `theme` sudah ada di schema sejak awal (belum pernah dipakai); `accentColor` baru ditambahkan dengan `.default('navy')` di Zod sehingga data lama otomatis dapat nilai default tanpa migrasi manual.
- **Ilustrasi**: karena tidak ada aset ilustrasi pihak ketiga yang cocok dengan prinsip zero-third-party-request proyek ini (font pun sengaja di-self-host karena alasan yang sama, lihat komentar CON-06/SEC-02 di `theme.css`), 4 ilustrasi SVG dibuat sendiri dari nol (bukan diambil dari unDraw/Storyset dsb): `EmptyActivities`, `EmptyEvidence`, `NoSearchResults`, `WelcomeHero` di `src/components/illustrations/`, memakai `hsl(var(--primary))`/`hsl(var(--border))`/dst secara langsung (bukan class Tailwind) supaya otomatis ikut tema+aksen aktif. `EmptyState` diberi prop `illustration?` opsional (default kosong, tidak mengubah pemakaian lama). Dipasang di: empty state Kegiatan, empty state Evidence Inbox, hasil kosong Pencarian Global, dan hero halaman Onboarding. Dashboard mendapat dekorasi lingkaran lembut (bukan komponen ilustrasi terpisah -- band gradient di header Dashboard sudah ada dari polesan sebelumnya, tinggal ditambah SVG dekoratif restrained di dalamnya) sesuai prinsip "Data-Dense Dashboard" yang tidak menyarankan dekorasi berlebihan di layar padat data.
- Diverifikasi lewat browser langsung: mode gelap/terang/otomatis (persist ke IndexedDB + terapkan ke DOM), kelima swatch aksen (dicek kontras warna primary-foreground vs primary secara manual per swatch), keempat ilustrasi (dicek `viewBox` masing-masing benar-benar ter-render di halaman targetnya lewat query DOM), serta `read_network_requests` mengonfirmasi nol permintaan pihak ketiga. Juga `npm run typecheck && npm run lint && npm run test -- --run && npm run build` seluruhnya bersih (104 test lulus).

[2026-08-17] Lima permintaan fitur tambahan pasca-redesign: (1) isi banyak link bukti dukung sekaligus, (2) pemilihan RK massal untuk kegiatan hasil import PDF yang tidak auto-match, (3) warna berbeda per status kegiatan, (4) filter bulan di Dashboard, (5) ganti `<input type="month">` native (dirasa "kaku") dengan picker custom yang bukan kalender harian.
- **(1)** `BuktiTautanPage.tsx` ternyata sudah punya ~90% fitur ini (multi-select checkbox + dialog tempel-tautan-massal, `applyBulkPaste`/`confirmLinks`) dari fase sebelumnya -- tinggal ditambah dua hal: tombol "Isi link yang sama untuk semua" (satu input URL + terapkan ke seluruh `selected`, untuk kasus satu folder Drive dipakai banyak kegiatan) dan kontrol "Pilih semua di bulan ini" (`MonthPicker` + tombol, mengisi `selectedIds` dari activity yang tanggalnya masuk `monthRangeContaining`) di luar dialog supaya tidak perlu klik checkbox satu-satu -- sesuai contoh pengguna sendiri ("langsung satu bulan").
- **(2)** State per-draft (`pdfAutoMatches`/`pdfPlanOverrides`/`effectivePlanId`) di alur review import PDF (`BackupPage.tsx`) sudah lengkap per baris -- ditambah `unmatchedIndices` (baris terpilih yang `effectivePlanId`-nya masih null) dan bar kecil di atas tabel: hitung baris belum ber-RK + satu `RkCombobox` + tombol "Terapkan ke semua" yang men-set `pdfPlanOverrides` untuk seluruh `unmatchedIndices` sekaligus. Pilihan per baris (select native, sudah ada) tetap bisa di-override individual sesudahnya -- tidak ada perubahan pada `confirmPdfImport`.
- **(3)** `ActivityCard.tsx`: badge status sebelumnya selalu `bg-secondary` datar untuk kelima status (`draft`/`complete`/`ready_to_report`/`reported`/`archived`) -- diberi `STATUS_BADGE_CLASS` per status memakai token semantik yang sudah ada (`muted`/`info`/`warning`/`success`, pola sama seperti `text-success`/`text-warning` untuk indikator link bukti dukung di file yang sama), bukan warna hex baru. Catatan: pengguna menyebut "kosong/draft/lengkap" tapi enum sebenarnya tidak punya status "kosong" terpisah -- kelima status nyata diberi warna berbeda.
- **(4) & (5)** Dibuat `src/components/common/MonthPicker.tsx` -- Popover kustom (tombol trigger "Agustus 2026" + panel stepper tahun di atas grid 3x4 nama bulan singkat) menggantikan `<input type="month">` di tiga tempat: `LaporanPage.tsx`, `KegiatanPage.tsx` (mode `clearable`, ada opsi "Semua bulan"), dan baru di `DashboardPage.tsx`. `MONTH_NAMES_ID`/`MONTH_NAMES_ID_SHORT` dipindah jadi export dari `src/lib/date/date-utils.ts` (sebelumnya duplikat privat di banyak file) supaya `MonthPicker` dan `DashboardPage` pakai satu sumber. Dashboard: `year`/`month`/`skpPeriod` yang sebelumnya selalu dari `todayString()` sekarang berasal dari state `monthAnchor` (default bulan berjalan); `today` tetap dipakai terpisah untuk batas "terisi hingga hari ini" -- keputusan desain: batas ini HANYA dipakai kalau `monthAnchor` sama dengan bulan berjalan sungguhan, bulan lain (masa lalu/depan) dihitung penuh sebulan karena konsep "sejauh ini" tidak relevan untuk bulan yang bukan sekarang. `selectedDate` di-reset saat `monthAnchor` berubah. `ActivityHeatmap` (rolling 12 bulan) dan `WeeklyReviewCard` (minggu berjalan) sengaja TIDAK ikut `monthAnchor` -- di luar cakupan permintaan, keduanya punya jendela waktu sendiri yang independen dari bulan yang sedang dilihat di kalender/statistik.
- Diverifikasi lewat browser: MonthPicker di Dashboard/Laporan/Kegiatan/Bukti&Tautan (popover grid bulan, bukan kalender harian, berhasil pindah bulan dan update data terkait); "isi link yang sama" mengisi kedua baris terpilih dengan URL sama dan tersimpan ke IndexedDB dengan status `uploaded`; "pilih semua di bulan ini" memilih tepat 2 kegiatan bertanggal Agustus 2026; badge status kegiatan Draft tampil dengan kelas warna baru (`bg-muted text-muted-foreground`) -- keempat status lain tidak diverifikasi visual langsung (tidak ada data seed dengan status tersebut untuk dites), diverifikasi lewat pembacaan kode/kelas Tailwind saja. Fitur bulk-RK import PDF diverifikasi lewat `typecheck`/`lint`/`build` dan pembacaan alur kode (tidak ada berkas PDF sampel tersedia di lingkungan ini untuk uji end-to-end interaktif). Seluruh 104 test tetap lulus, build bersih.

[2026-08-17] Pengguna meminta KipLog dikunci hanya untuk dirinya sendiri, dengan data yang sama di semua perangkat. Diklarifikasi dan disepakati: login pakai akun Google pribadi pengguna (satu email saja yang diizinkan) + Google Drive milik pengguna sebagai backend sinkronisasi. Ini **pengecualian yang disengaja** terhadap prinsip "nol permintaan pihak ketiga" (SEC-08/CON-06) yang dipegang ketat sejak awal proyek ini (font di-self-host, dst.) — bukan pelanggaran diam-diam, melainkan permintaan eksplisit pengguna dengan konsekuensi yang sudah dijelaskan ke pengguna sebelum dikerjakan.
- **Otentikasi**: Google Identity Services (`accounts.google.com/gsi/client`), token client OAuth2 dengan scope `drive.appdata` saja (scope "non-sensitif" -- tidak perlu proses verifikasi aplikasi dari Google selama status publikasi consent screen tetap "Testing"). Setelah dapat access token, email pemiliknya diverifikasi lewat endpoint `userinfo` Google (tidak bisa dipalsukan dari sisi client) dan dibandingkan dengan satu email yang diizinkan (`VITE_ALLOWED_EMAIL`). **Dua lapis proteksi "hanya saya"**: (1) consent screen Google Cloud tetap berstatus Testing dengan HANYA email pengguna sebagai test user -- Google sendiri menolak siapa pun selain itu sebelum kode aplikasi sempat berjalan; (2) pengecekan email di sisi aplikasi sebagai lapis kedua.
- **Akses offline**: mewajibkan verifikasi Google setiap kali aplikasi dibuka akan mengunci pengguna dari data offline miliknya sendiri -- bertentangan dengan sifat offline-first aplikasi ini. Karena itu, `useAuth` (`src/hooks/useAuth.ts`) menyimpan cache `{email, verifiedAt}` di `localStorage` setelah verifikasi pertama berhasil; pembukaan berikutnya dengan email cache yang cocok langsung diizinkan (bisa offline), sambil mencoba refresh token diam-diam di latar belakang untuk sinkronisasi. Perangkat/browser baru tanpa cache tetap wajib login Google sungguhan sekali (butuh internet) -- tidak terhindarkan dan wajar.
- **Model sinkronisasi**: snapshot utuh, BUKAN merge per-record. Memakai ulang `exportBackupZip()`/`parseBackupFile()`/`executeImport()` yang sudah ada di `src/lib/services/backup.ts` (sudah mencakup 8 tabel Dexie + blob bukti dukung + `exportedAt` bertingkat versi) sebagai format payload sinkron -- tidak menulis ulang mesin backup dari nol. Saat aplikasi dibuka (`pullOnStart`, dipanggil dari `RequireAuth.tsx`): unduh snapshot dari folder tersembunyi `appDataFolder` di Drive akun pengguna (bukan folder Drive biasa yang terlihat); kalau `exportedAt` di Drive lebih baru dari penanda lokal terakhir, `executeImport(payload, 'replace')` dijalankan SEBELUM data ditampilkan. Push terjadi lewat: debounce 8 detik setelah perubahan lokal terdeteksi (`src/db/database.ts` memasang `hook('creating'|'updating'|'deleting')` di kedelapan tabel, memanggil callback yang didaftarkan lewat `setOnLocalWrite` -- pola ini sengaja menghindari import langsung `sync-engine.ts` di `database.ts` karena akan membuat siklus impor lewat `backup.ts` -> repositories -> `database.ts`), saat tab disembunyikan (`visibilitychange`), dan tombol manual "Sinkron Sekarang" di Pengaturan > Sinkronisasi. **Keterbatasan yang didokumentasikan, bukan direkayasa hilang**: karena ini "snapshot terakhir menang" bukan merge sungguhan, mengedit di dua perangkat pada saat bersamaan akan kehilangan salah satu sisi (siapa yang push belakangan) -- wajar untuk kasus pemakaian satu-orang-bergantian-perangkat yang diminta, bukan multi-editor bersamaan; membangun CRDT/merge per-record di luar cakupan.
- **CSP** (`index.html`, tag SEC-08) dilonggarkan HANYA untuk domain Google: `script-src`/`connect-src` untuk `accounts.google.com`/`www.googleapis.com`/`oauth2.googleapis.com`, `frame-src` baru untuk `accounts.google.com`, `img-src` untuk `*.googleusercontent.com` (foto profil). Tidak ada domain lain yang ditambahkan.
- **Prasyarat dari pengguna** (tidak bisa dikerjakan otomatis oleh asisten): membuat proyek Google Cloud sendiri, mengaktifkan Google Drive API, membuat OAuth consent screen (External, status Testing, hanya email pengguna sebagai test user), membuat OAuth Client ID (Web, Authorized JavaScript origins `http://localhost:5173` dan `https://flameheater.github.io`) -- langkah lengkap ada di `.env.example`. Client ID + email lalu diisi ke `VITE_GOOGLE_CLIENT_ID`/`VITE_ALLOWED_EMAIL`: `.env.local` untuk dev lokal (sudah tercakup pola `*.local` di `.gitignore`), dan sebagai GitHub Actions repo secret untuk build produksi (`.github/workflows/deploy.yml` sudah diperbarui untuk menyuntikkan keduanya).
- Diverifikasi: tanpa env var terisi, aplikasi gagal-aman terkunci total (menampilkan layar "Akun ini tidak diizinkan", tidak ada data yang bocor) -- dicek langsung di browser. Alur login Google + sinkronisasi Drive sungguhan BELUM bisa diuji end-to-end di lingkungan ini karena butuh Client ID asli dari Google Cloud Console milik pengguna (prasyarat di atas belum dipenuhi saat entri ini ditulis). `typecheck`/`lint`/104 test/`build` semuanya bersih.

[2026-08-17] Pengguna melaporkan: dua akun Google yang diizinkan masuk (fadlyakbar5121@gmail.com dan fadlyakbar209@gmail.com) tidak berbagi data yang sama -- akun kedua diminta onboarding lagi seolah pengguna baru. Penyebab: sinkronisasi sebelumnya memakai `appDataFolder` Drive, yang bersifat PRIVAT PER AKUN GOOGLE -- folder tersembunyi milik akun A tidak pernah bisa dilihat akun B sama sekali, walau keduanya sama-sama "diizinkan" login ke aplikasi. Diklarifikasi dan disetujui pengguna: pindah ke satu **folder Drive biasa (bukan appDataFolder)** yang dibuat di salah satu akun lalu dibagikan (share, akses Editor) ke akun lainnya, sehingga file sinkron yang sama benar-benar bisa diakses kedua akun.
- **Konsekuensi yang disetujui pengguna**: scope OAuth diperluas dari `drive.appdata` (non-sensitif) menjadi `drive` penuh (sensitif) -- appdata tidak bisa dipakai untuk file yang dibagikan pengguna lain, hanya `drive` scope yang bisa melihat file yang di-share ke akun tersebut. Konsekuensinya, layar consent Google sekarang menampilkan peringatan "aplikasi belum diverifikasi" yang perlu dilewati manual (klik "Advanced > Go to [app] (unsafe)") -- ini NORMAL untuk aplikasi status Testing yang belum di-publish (Google tidak mewajibkan proses verifikasi selama masih Testing, apa pun scope-nya; verifikasi hanya wajib saat status diubah ke "In production").
- **Implementasi**: `src/lib/sync/drive-client.ts` diubah dari query `spaces=appDataFolder` menjadi query berbasis `'<FOLDER_ID>' in parents` terhadap folder ID baru (`VITE_SYNC_FOLDER_ID`, env var baru) -- file `kiplog-sync.zip` yang sama di folder itu jadi satu-satunya sumber kebenaran untuk SEMUA akun yang diizinkan, bukan satu file terpisah per akun. `supportsAllDrives`/`includeItemsFromAllDrives` ditambahkan di query supaya tetap kompatibel kalau folder itu suatu saat ada di Shared Drive, bukan cuma My Drive. `.env.example` diperbarui dengan langkah manual yang wajib dilakukan pengguna: buat folder di salah satu akun, share ke akun lain (Editor), salin ID folder dari URL, isi ke `VITE_SYNC_FOLDER_ID`.
- Diverifikasi: `typecheck`/`lint`/104 test/`build` semuanya bersih. Alur sinkronisasi folder-bersama yang sesungguhnya BELUM diuji end-to-end (butuh pengguna membuat+membagikan folder Drive sungguhan dan memberi ID-nya terlebih dulu -- belum dilakukan saat entri ini ditulis).

[2026-08-17] Pengguna meminta data sinkron di Drive tidak berupa satu berkas zip buram, melainkan berkas/folder yang tertata dan bisa dibuka langsung di Drive -- dikonfirmasi penyimpanan LOKAL (IndexedDB) tetap dipertahankan (bukan "cache sementara" yang boleh dibuang, itu penyimpanan utama offline-first aplikasi ini), yang diubah hanya representasi di sisi Drive.
- **Struktur baru** di dalam folder sinkron bersama (`VITE_SYNC_FOLDER_ID`): `kiplog-data.json` (payload terstruktur, sama seperti isi `data.json` di backup ZIP sebelumnya -- bisa dibuka & dibaca langsung) + subfolder `evidence/` berisi berkas asli per item bukti dukung (`<id>.<ext>` + `<id>_thumb.webp`), bukan lagi satu `kiplog-sync.zip` opak.
- **Implementasi**: `src/lib/services/backup.ts` -- `collectPayload`, `extensionFromMimeType`, `migratePayload` diubah dari privat jadi `export` supaya bisa dipakai ulang oleh mesin sinkron tanpa duplikasi logika (fungsi backup ZIP/JSON manual di halaman Backup & Import tidak berubah sama sekali, tetap dipakai apa adanya). `src/lib/sync/drive-client.ts` ditulis ulang: dari satu fungsi upload/download file tunggal menjadi klien dengan operasi per-berkas (`uploadDataFile`/`downloadDataFile` untuk `kiplog-data.json`, `uploadEvidenceFile`/`downloadEvidenceFile`/`listEvidenceFileNames` untuk isi subfolder `evidence/`, dengan folder `evidence/` dicari-atau-dibuat sekali per sesi dan di-cache di memori). `src/lib/sync/sync-engine.ts` -- `push()`/`pull()` ditulis ulang mengikuti struktur baru ini.
- **Optimasi bukti dukung**: karena berkas bukti dukung jarang berubah setelah diunggah, ditambahkan penanda `kiplog-evidence-synced-ids` di localStorage (per perangkat) yang mencatat ID bukti dukung yang sudah pernah naik/turun ke Drive -- push berikutnya hanya mengunggah item yang BELUM ada di penanda ini, bukan mengunggah ulang seluruh galeri bukti dukung setiap kali debounce sinkron berjalan (8 detik setelah perubahan lokal) -- penting untuk menghindari boros kuota API Drive dan membuat sinkron tetap cepat pada galeri bukti dukung yang besar. Saat pull, ID yang berhasil diunduh juga otomatis ditandai tersinkron di perangkat itu, supaya perangkat itu tidak mencoba mengunggah ulang berkas yang sebenarnya sudah ada di Drive (hasil pull sebelumnya).
- **Catatan migrasi**: berkas `kiplog-sync.zip` lama (dari implementasi appDataFolder/folder-bersama sebelum entri ini) tidak lagi dibaca kode baru dan akan jadi berkas yatim di folder Drive lama jika sempat sudah dibuat -- aman dihapus manual oleh pengguna, tidak memengaruhi data lokal.
- Diverifikasi: `typecheck`/`lint`/104 test/`build` semuanya bersih. Alur push/pull struktur baru terhadap folder Drive sungguhan BELUM diuji end-to-end langsung oleh asisten di sesi ini (lingkungan pengujian browser di sini tidak memiliki token akses Google yang valid -- login sungguhan hanya pernah dilakukan pengguna di browser mereka sendiri) -- perlu diverifikasi pengguna langsung: buka folder Drive setelah sinkron berjalan, konfirmasi `kiplog-data.json` dan subfolder `evidence/` muncul dan bisa dibuka.

[2026-08-17] Pengguna memberikan `202603 Cascading Kinerja 5108 Tahun 2026.xlsx` dan meminta analisis mendalam atas kegiatan apa saja yang termasuk dalam masing-masing RK, supaya pemilihan RK otomatis dari deskripsi kegiatan jadi tepat. **Temuan pertama yang mengubah lingkup: file itu tidak memuat daftar kegiatan per RK.** Perinciannya berhenti di Nama Proyek ("Kegiatan Statistik Harga"), bukan kegiatan harian; kolom Indikator Kinerja Anggota Tim adalah yang paling mendekati (menyebut jenis luaran yang dihitung dalam satu RK), dan kolom tingkat Anggota Tim bahkan kosong untuk sebagian tim (Tim Statistik Harga hanya terisi di tingkat Ketua Tim) sehingga 7 dari 40 RK pengguna tidak punya baris padanan. Karena itu istilah kegiatan sehari-hari diambil dari sumber kedua yang ternyata ada di perangkat pengguna: PDF Data Dukung April 2026 (46 kegiatan, 14 sudah berpasangan dengan RK — data BERLABEL) dan PDF Rincian Aktivitas Juli–Agustus 2026 (39 deskripsi). Tidak ada "training" model apa pun di sini; yang dilakukan adalah kurasi kosakata + kalibrasi bobot deterministik, sesuai CON-02 (tanpa LLM).
- **Baseline diukur lebih dulu, bukan diasumsikan**: matcher lama ternyata presisinya sudah baik (13 dari 14 kegiatan berlabel tepat di posisi teratas) tetapi hanya memberi rekomendasi untuk 27 dari 85 deskripsi (32%). Jadi masalahnya recall, bukan presisi — arah perbaikan berubah karena pengukuran ini.
- **Tiga cacat struktural** yang ditemukan: (1) kecocokan SATU kata kunci bernilai 32 sementara ambang tampil 35, sehingga kata sekhas "sakernas"/"shped"/"upacara" secara matematis tidak pernah bisa memunculkan RK-nya sendirian; (2) kemiripan nama RK dihitung atas seluruh teks nama padahal nama RK BPS memakai kerangka kalimat yang sama — terukur 12 token muncul di >= 15% dari 130 nama RK resmi, dan akibatnya "Statistik Harga" vs "Statistik Jasa" mencapai kemiripan 0,92 tanpa berbagi bidang sama sekali (dikonfirmasi: matcher pembanding yang saya tulis sendiri untuk memetakan file cascading pun salah kaitkan Harga ke Jasa karena sebab yang sama); (3) kata kunci berupa frasa panjang pecah begitu pengguna menyelipkan satu kata ("rilis neraca pengeluaran" gagal untuk "rilis BRS neraca pengeluaran").
- **Perbaikan**: pencocokan jadi dua sumbu mengikuti struktur file cascading — BIDANG (Tim Kerja/Nama Proyek, 22 bidang) dan BENTUK PEKERJAAN (Indikator Kinerja: pelaksanaan/publikasi/rilis/administrasi). Bobot kata kunci sekarang mengikuti kekhasannya (dibagi jumlah RK pemilik, dihitung saat itu juga dari daftar RK sehingga otomatis menyesuaikan saat pengguna menyunting kata kunci). Kosakata diperluas dari 239 kata kunci hasil kurasi. Ambang tetap 35 dengan konsekuensi yang disengaja: satu kata kunci khas cukup sendirian, bidang saja TIDAK cukup (25 < 35) supaya deskripsi yang hanya menyebut bidangnya memunculkan beberapa kandidat alih-alih satu jawaban percaya diri yang berpeluang salah. Rincian lengkap di `docs/RK-MATCHING.md`.
- **Frasa bidang polos** ("neraca produksi", "neraca pengeluaran", "opd") dikeluarkan dari daftar kata kunci RK: bidang sudah punya sinyal sendiri, dan karena hanya SATU dari empat RK Neraca Pengeluaran yang mencantumkan "neraca pengeluaran" sebagai kata kunci, RK itu tampak paling khas hanya karena bidangnya disebut dan mengalahkan RK sebidang yang bentuk pekerjaannya justru cocok. Percobaan menyelesaikan ini di sisi kode (membuang semua kata kunci yang sama dengan istilah bidang) DITOLAK setelah diuji — aturan itu ikut membuang penciri tunggal yang sah seperti `sktnp`, `romantik`, `desa cantik`, dan `quality gate`, membuat 5 test gagal; masalah data diselesaikan di data.
- **`applyLocalLearning` disaring**: pembelajaran lokal dulu menyimpan SEMUA token deskripsi, termasuk "rapat"/"melakukan"/"kantor". Dengan pembobotan kekhasan yang baru, kata umum yang tersimpan di satu RK saja akan dinilai sebagai penciri sangat kuat — persis kebalikan kenyataannya. Ditambah daftar `GENERIC_ACTIVITY_WORDS` + minimal 4 huruf + bukan angka.
- **Hasil terhadap 85 deskripsi nyata**: tepat di posisi teratas 13/14 -> **14/14**; cakupan 27/85 (32%) -> **79/85 (93%)**. Enam yang tetap abstain memuat istilah yang belum dikenal (LTB, Simfoni, pembayaran termin, "Halal bi Halal") — abstain dipilih daripada menebak, dan pembelajaran lokal akan menyerapnya setelah pengguna memilih sekali.
- **Ketidakpastian yang TIDAK disembunyikan**: `sktip` dipetakan ke Neraca Pengeluaran atas dugaan kepanjangannya (kalau salah, pengguna bisa memindahkannya lewat KeywordEditor); `pantastik` dan `shanti` diambil dari log kegiatan tanpa sumber resmi, dipasang ke bidang tempat keduanya muncul; `ltb` sengaja TIDAK dipetakan karena kepanjangannya belum pasti.
- **Jalur agar perbaikan ini sampai ke data yang sudah ada**: `seedPerformancePlansIfEmpty` hanya jalan saat tabel kosong, jadi pemasangan pengguna yang sudah berisi 40 RK tidak akan pernah menerima kata kunci baru. Ditambah `refreshSeedKeywords()` + tombol "Perbarui kata kunci bawaan" di halaman Rencana Kinerja, MENGGABUNG bukan menimpa (kata kunci buatan pengguna dan hasil pembelajaran tetap utuh; konsekuensi yang disadari: kata kunci bawaan yang pernah dihapus pengguna akan muncul kembali).
- Diverifikasi: `npm run verify` bersih (typecheck + lint + 123 test + build), 19 test baru termasuk 10 istilah lapangan, pemisahan Harga vs Jasa, pemisahan rilis vs pelaksanaan sebidang, dan penyaringan pembelajaran. **BELUM diverifikasi langsung di browser**: dev server pengguna berjalan di port 5173 tetapi aplikasi terkunci di layar login Google, dan asisten tidak memasukkan kredensial akun pengguna. Panel rekomendasi RK di form Kegiatan serta tombol "Perbarui kata kunci bawaan" perlu diperiksa pengguna sendiri setelah login. Korpus evaluasi (85 deskripsi kegiatan pribadi) sengaja TIDAK di-commit; hanya kasus yang mewakilinya masuk ke test.

[2026-08-17] Pengguna meminta fitur otomatisasi pemindahan isian KipLog ke KipApp yang sebenarnya. Awalnya ia hendak menyambungkan Chrome-nya agar struktur DOM form KipApp asli bisa dibaca, lalu berubah pikiran dan meminta cukup dipelajari dari buku panduan. **Larangan CON-03 dibacakan kembali kepadanya lebih dulu** (dilarang scraping/headless-browser/login otomatis ke KipApp) beserta konsekuensi bahwa PRD dan README perlu direvisi bila fitur ini dibuat; pengguna menegaskan pilihannya, jadi dikerjakan penuh dan dokumennya ikut direvisi — CON-03 diberi catatan revisi dan CON-03a baru ditambahkan sebagai batas pengecualiannya, bukan dibiarkan bertentangan diam-diam.
- **Sumber**: `Panduan KipApp - Pengguna V.3.1.pdf`. Teks panduan saja tidak cukup (hanya menyebut urutan field dalam satu kalimat di hal. 65), jadi halaman 64–67 dirender jadi PNG lewat pymupdf dan **tangkapan layar dialognya dibaca langsung**. `pdftoppm`/poppler tidak tersedia di lingkungan ini sehingga `Read` pada PDF gagal; pymupdf dipasang lewat pip sebagai gantinya.
- **Temuan dari tangkapan layar hal. 66 (dialog "Add Capaian Kegiatan Perhari") yang MEMBANTAH asumsi kode lama** — empat label mode salin salah dan satu format nilai tidak bisa dipakai sama sekali: field Tanggal adalah input `date` berisi `2022-12-05` (ISO), sedangkan `buildCopyModeFields` menyalin hasil `formatIndonesianDate` ("5 Desember 2022") yang MUSTAHIL ditempel ke field itu; labelnya "Kegiatan" bukan "Deskripsi"; "Progres" (satu huruf s) bukan "Progress"; "Data Dukung" bukan "Link Bukti Dukung"; dan status capaian bukan field teks melainkan checkbox "Masukan ke capaian SKP" sehingga menempelkan kalimat "Masuk capaian SKP" ke sana tidak ada artinya. Kelimanya diperbaiki — ini bug nyata yang berdiri sendiri, terlepas dari fitur autofill.
- **Temuan lain**: Rencana Kinerja TERNYATA ada sebagai select di dalam dialog (teks panduan hal. 65 langkah d–e hanya menyebut pemilihan RK di halaman, sehingga dugaan awal saya keliru bahwa RK murni konteks halaman) — jadi autofill bisa memilihnya juga, dengan mencocokkan teks opsi ke `plan.name`. Ini membuat aturan verbatim DR-01 punya konsekuensi teknis baru: nama RK yang "dirapikan" tidak akan cocok dengan opsi di KipApp. Panduan hal. 68 juga menegaskan capaian yang sudah dikirim untuk dinilai tidak bisa dibatalkan/diedit, jadi `autofillBlockedReason()` menolak kegiatan dengan `sentForReview`.
- **Bentuk yang dipilih**: bookmarklet, bukan ekstensi atau otomasi headless. Dijalankan pengguna dengan tangannya sendiri di tab KipApp yang sudah ia login, menolak berjalan di host selain `webapps.bps.go.id`, hanya MENULIS ke field, dan **tidak menekan Save** — pencocokan field bertumpu pada teks label yang terlihat dan tata letak KipApp bisa berubah tanpa pemberitahuan, jadi pemeriksaan akhir tetap milik pengguna. Field yang gagal ditemukan dilaporkan di panel, tidak didiamkan. Skrip ES5 utuh-sendiri tanpa memuat berkas luar karena CSP KipApp hampir pasti memblokir itu.
- **Diverifikasi tanpa menyentuh KipApp asli**: tiruan dialog dibangun di jsdom mengikuti tangkapan layar (label sebagai elemen terpisah, tanda bintang wajib, tiga field teratas hanya teks baca), lalu `AUTOFILL_SCRIPT` yang SEBENARNYA dikirim dijalankan lewat `new Function` dengan URL jsdom diset ke host KipApp. 15 test, termasuk: kesembilan field terisi, event `input`+`change` benar-benar terpicu (supaya framework KipApp memperbarui state-nya), jam kosong dilewati bukan diisi string kosong, RK yang tidak ada di opsi dilaporkan, dan **tombol Save tidak pernah terklik**.
- **Yang TIDAK bisa diverifikasi**: nama atribut DOM asli (`id`/`name`/`class`) tidak diketahui karena tangkapan layar hanya memperlihatkan tampilan, dan pengguna membatalkan rencana menyambungkan Chrome. Karena itu pencocokan sengaja berbasis teks label yang terlihat — satu-satunya penanda yang benar-benar diketahui. Konsekuensinya: **fitur ini wajib diuji pengguna sekali di form KipApp sungguhan sebelum dipercaya**; kalau ada field yang dilaporkan gagal, kirimkan pesan gagalnya untuk penyesuaian. Jam 5173 dev server pengguna berjalan tetapi aplikasi terkunci di layar login Google, jadi tampilan kartu autofill di KipLog juga belum diperiksa langsung.

[2026-08-18] Pengguna mengirim tangkapan layar form KipApp yang SUNGGUHAN (v2.0.4, tahun 2026, dialog "Add Capaian Kegiatan Perhari"). Panduan yang dipakai sehari sebelumnya bertanggal 2022 dan formnya sudah berubah — **dua perbedaan membuat autofill versi pertama gagal sebagian, bukan sekadar kurang rapi**:
- **Dua checkbox baru "Gunakan periode tanggal" dan "Gunakan jam"** yang tidak ada sama sekali di panduan 2022. Yang kedua penting: **field Jam Mulai/Jam Selesai tidak ada di DOM sampai checkbox itu dicentang**, jadi skrip lama pasti melaporkan keduanya "tidak ditemukan". Sekarang skrip mencentang "Gunakan jam" lebih dulu bila payload punya jam, dan sengaja MEMBIARKANNYA tidak tercentang bila pengguna memilih tidak mencatat jam — cocok dengan fitur jam opsional KipLog. "Gunakan periode tanggal" selalu dipastikan tidak tercentang karena kegiatan KipLog selalu satu hari.
- **Tanggal bukan input `date` bawaan browser** melainkan pemilih buatan sendiri (placeholder "Pilih tanggal", bukan contoh nilai ISO seperti di panduan). Karena pemilih semacam ini umumnya baru mengambil nilai saat kehilangan fokus atau Enter, skrip kini mengirim `keydown` Enter + `blur` sesudah menulis nilai. **Format tanggal yang diterima belum terkonfirmasi** — tangkapan layar memperlihatkan field kosong, jadi ISO dipakai atas dasar panduan 2022 dan pesan hasil sekarang secara khusus meminta pengguna memeriksa field Tanggal.
- Tiga hal lain dari panduan 2022 **terkonfirmasi masih benar** di form 2026: label "Kegiatan"/"Progres"/"Data Dukung" (placeholder-nya "Deskripsi Kegiatan"/"Deskripsi Capaian"/"Link Data Dukung") dan "Masukan ke capaian SKP" sebagai checkbox — yang ternyata bawaannya TIDAK tercentang.
- **Dua bug pencocokan yang tampak setelah tiruan test disesuaikan ke markup 2026**: (1) tanda bintang wajib adalah elemen tersendiri di dalam label, sehingga syarat "elemen tanpa anak" membuat SELURUH field wajib tidak ditemukan — diganti dengan memilih elemen TERKECIL yang teksnya pas; (2) dua checkbox pengalih duduk di baris yang sama, sehingga memilih "kontrol pertama yang ketemu di leluhur" akan mencentang checkbox yang salah — diganti dengan memilih kontrol yang jarak DOM-nya paling dekat dengan teks label.
- **Pembatasan lingkup ke dialog**: sidebar KipApp memuat menu "Rencana Kinerja" dan halaman di belakang dialog punya filter berlabel "Rencana Kinerja" + "Tanggal" yang sama persis. Tanpa pembatasan, autofill bisa mengisi filter halaman alih-alih formnya. Skrip sekarang mencari elemen terkecil yang memuat judul dialog sekaligus setidaknya satu kontrol form, lalu mencari hanya di dalamnya — ada test khusus dengan sidebar+filter tiruan yang membuktikan kontrol berlabel sama di luar dialog tetap kosong.
- **Periode SKP di KipApp 2026 triwulanan** ("1 April - 30 Juni (Triwulan II)"), sedangkan panduan 2022 menunjukkan bulanan dan KipLog memodelkan `skpPeriod` sebagai `YYYY-MM`. Tidak berpengaruh pada autofill (field SKP hanya teks baca), tetapi berpengaruh pada arti "Kunci Periode" di halaman KipApp Ready dan pada rekonsiliasi Excel. TIDAK diubah di sini karena menyentuh skema data delapan tabel; dicatat sebagai temuan terbuka.
- Diverifikasi: `npm run verify` bersih (143 test). Tiruan dialog di jsdom sekarang mengikuti markup 2026, bukan panduan 2022. Yang MASIH belum diverifikasi: format tanggal yang benar-benar diterima pemilih tanggal KipApp, dan apakah nama atribut/struktur asli cocok dengan heuristik label — perlu satu kali uji pengguna di form sungguhan.

[2026-08-18] Pengguna memberi tahu alamat KipApp yang ia pakai: `https://kipapp.bps.go.id/`. PRD CON-03 menyebut `https://webapps.bps.go.id/kipapp/`, dan penjaga host bookmarklet dibangun dari alamat PRD itu — artinya bookmarklet yang baru saja dipush **akan menolak berjalan** di alamat yang sebenarnya dipakai pengguna, gagal total dan bukan gagal sebagian. Kedua alamat diperiksa dengan curl: **keduanya menjawab HTTP 200**, jadi keduanya dimasukkan ke daftar host yang diizinkan alih-alih menggantikan yang satu dengan yang lain — tidak ada dasar untuk menebak mana yang akan dimatikan lebih dulu. `KIPAPP_HOST` (tunggal) diganti `KIPAPP_HOSTS` (daftar), CON-03a dan README ikut diperbarui. Ditambah berkas test terpisah `kipapp-autofill-host.test.ts` (URL jsdom diset ke host yang bukan KipApp, karena URL lingkungan ditentukan per berkas) yang membuktikan skrip menolak berjalan, tidak menyuntikkan panel, tidak menyentuh field, dan menyebut kedua host yang sah di pesan penolakannya. 146 test lulus, `npm run verify` bersih.

[2026-08-18] Pengguna mencoba autofill di form KipApp sungguhan dan melaporkan dua field gagal, disertai tangkapan layar: Rencana Kinerja dan Tanggal. Penyebabnya sama — **keduanya bukan kontrol HTML biasa, melainkan komponen yang baru memunculkan isinya setelah diklik**, sehingga menulis nilai langsung ke kontrolnya tidak berpengaruh apa pun. Ini tidak bisa terlihat dari tangkapan layar statis sebelumnya (yang memperlihatkan field dalam keadaan tertutup), dan tidak bisa terlihat dari tiruan jsdom lama yang memakai `<select>` dan `input type=date` biasa — **tiruan yang salah membuat test hijau padahal produksinya gagal**. Tiruan sekarang meniru PERILAKUNYA, bukan tampilannya.
- **Rencana Kinerja**: combobox. Klik membuka daftar, mengetik menyaring, dan pemilihan hanya terjadi kalau OPSINYA DIKLIK. Skrip sekarang: klik kontrol, ketik nama RK untuk menyaring, lalu cari elemen mana pun di halaman yang teksnya sama persis dan klik elemen itu. Test khusus memastikan yang diperiksa adalah "benar-benar terpilih" (atribut yang hanya terisi lewat klik opsi), bukan sekadar "kotaknya terlihat terisi".
- **Tanggal**: klik membuka kalender yang punya kotak isian sendiri di atas grid tanggalnya. Tangkapan layar pengguna memperlihatkan kotak itu berisi `2026-04-01` — **ini sekaligus menjawab pertanyaan format yang sebelumnya terbuka: ISO, dan dugaan dari panduan 2022 ternyata benar**. Skrip sekarang: klik kontrol, tulis ke input yang BARU muncul (dideteksi dengan membandingkan daftar input terlihat sebelum dan sesudah klik), lalu Enter.
- **Konsekuensi arsitektur**: pengisian tidak bisa lagi satu jalan lurus karena popup muncul belakangan. Skrip berubah jadi rangkaian langkah berjeda 300 ms (`AUTOFILL_STEP_DELAY_MS`), dengan pesan "Mengisi…" selama berjalan. Kegagalan satu langkah tidak menghentikan sisanya.
- **Cacat yang ketahuan dari test yang gagal**: kalau nama RK tidak ada di daftar, teks pencarian tertinggal di kotak RK sehingga tampak terisi padahal tidak ada yang terpilih — lebih menyesatkan daripada kotak kosong. Sekarang teks itu dibersihkan dan Escape ditekan sebelum melaporkan gagal.
- Jam Mulai/Jam Selesai diperlakukan dengan rutin popup yang sama, dengan jatuh-balik menulis langsung bila tidak ada popup yang muncul — belum terkonfirmasi apakah keduanya pemilih popup juga.
- `visible()` di skrip memakai `offsetWidth/offsetHeight`, yang di jsdom selalu nol; test menambal properti itu supaya yang diuji logika skripnya, bukan keterbatasan jsdom. Konsekuensinya: **penyaringan "hanya elemen terlihat" tidak benar-benar teruji** dan hanya terbukti di browser sungguhan.
- Diverifikasi: `npm run verify` bersih, 148 test (22 di berkas autofill). Yang masih perlu percobaan pengguna: apakah jeda 300 ms cukup untuk popup KipApp di jaringan/perangkat nyata, dan apakah field jam juga berupa pemilih popup.

[2026-08-18] Percobaan kedua pengguna di form sungguhan: Rencana Kinerja tertulis tapi tidak terpilih, Tanggal tetap kosong, dan "Gunakan jam" tercentang padahal jamnya tidak terisi. **Cacat paling serius bukan ketiganya, melainkan bahwa panel melaporkan BERHASIL untuk semuanya** — skrip menandai sebuah field "terisi" begitu ia selesai mengklik, tanpa pernah memeriksa hasilnya. Memberi tahu pengguna berhasil padahal gagal lebih buruk daripada gagalnya sendiri. Tiga prinsip ditegakkan sebagai perbaikan:
- **Menunggu, bukan menebak waktu.** Jeda tetap diganti `waitFor` yang menunggu keadaan yang dituju sampai muncul (maks ~2,4 detik, interval 120 ms). Popup KipApp muncul kapan saja; jeda tetap 300 ms hanyalah tebakan.
- **Memeriksa, bukan mengaku.** Tiap field dibaca ulang setelah diisi (`holdsValue`, memeriksa `value` maupun teks di sekitar kontrol karena combobox menaruh pilihannya sebagai teks). Field yang tidak terbukti terisi masuk daftar "Gagal", bukan "Terisi".
- **Tidak meninggalkan form lebih buruk daripada saat ditemukan.** Mencentang "Gunakan jam" memunculkan DUA field wajib baru; kalau jamnya gagal diisi, centangnya dikembalikan dan alasannya dilaporkan. Inilah jawaban langsung atas keluhan ketiga pengguna.
- **Combobox**: klik, ketik untuk menyaring, TUNGGU opsinya muncul, klik opsinya. Verifikasi tidak boleh dipercaya kalau tidak ada opsi yang benar-benar diklik — teks yang barusan diketik sudah membuat kotaknya "berisi" nilai yang dicari, sehingga apa pun akan tampak berhasil. Ini ketahuan dari test yang gagal, bukan dari nalar di awal.
- **Pemilih tanggal**: coba tulis ke fieldnya sendiri lebih dulu, periksa; kalau tidak tercatat, coba kotak isian di dalam popup, periksa lagi. Tangkapan layar kalender pengguna memperlihatkan kotak itu berisi `2026-04-01` — **formatnya ISO, terkonfirmasi**.
- **Bug tersembunyi yang ditemukan test, lebih berbahaya dari gejala aslinya**: ketika sebuah field tidak ada (mis. pemilih jam yang tidak dikenali), pencarian kontrol merambat naik sampai menemukan kontrol APA PUN dan menulis nilainya ke field lain yang tidak berhubungan — diam-diam merusak isian yang sudah benar. Diperbaiki dengan aturan yang bisa dinalar dari tata letak: **tidak boleh ada label dikenal lain di antara label dan kontrol yang dipilih**. Dua aturan sebelumnya dicoba dan ditolak karena rapuh — "harus satu baris" (gagal untuk dua checkbox pengalih yang memang berbagi baris) dan "paling dekat ke labelnya sendiri" (gagal untuk label yang memuat elemen bintang wajib).
- **Tiruan test diperkuat lagi** supaya kegagalan produksi bisa tertangkap di test: dropdown dan kalender kini di-portal ke `<body>` seperti komponen sungguhan (bukan bersarang di dekat kontrolnya, yang membuat pemeriksaan bisa lolos palsu), dan field tanggal MENOLAK diketik langsung sehingga jalur popup benar-benar teruji. Ditambah dua test baru: centang jam dikembalikan saat jam gagal diisi, dan field yang nilainya tidak masuk tidak boleh muncul di daftar "Terisi".
- Diverifikasi: `npm run verify` bersih, 150 test (24 di berkas autofill). Yang masih belum terbukti tetap sama: perilaku di browser sungguhan, karena `visible()` memakai `offsetWidth` yang di jsdom selalu nol dan ditambal di test.

[2026-08-18] Percobaan ketiga pengguna: **Rencana Kinerja akhirnya BERHASIL terpilih** (jalur combobox klik-ketik-tunggu-klik terbukti benar di form sungguhan), dan laporan panel kini jujur — dua sisanya dilaporkan gagal, tidak lagi diaku berhasil. Dua kegagalan tersisa diperbaiki dari bukti yang terlihat di tangkapan layar, bukan tebakan:
- **"Tanggal (field tidak ditemukan)"** — penelusuran lewat label gagal di markup asli. Ditambah pencarian cadangan lewat **placeholder**, yang terlihat jelas di layar dan melekat pada kontrolnya sendiri: "Pilih tanggal", "Pilih jam", "Pilih rencana kinerja SKP". Kedua field jam memakai placeholder yang sama sehingga dibedakan urutannya (indeks 0 dan 1). Placeholder dipakai sebagai cadangan, bukan pengganti — label tetap dicoba lebih dulu karena lebih spesifik.
- **"Masukan ke capaian SKP (centang tidak berubah)"** — komponen checkbox menyembunyikan input aslinya di balik pembungkus yang menerima klik, sehingga mengklik input itu tidak berpengaruh. `toggleCheckbox` sekarang mencoba berlapis dan memeriksa setelah tiap lapis: input, pembungkusnya, induknya, lalu terakhir menyetel `checked` langsung beserta event-nya. Dipakai untuk ketiga checkbox, bukan hanya yang ini.
- **Tombol "Salin diagnosa"** ditambahkan ke panel supaya putaran perbaikan berikutnya berdasar bukti, bukan tebakan: ia melaporkan apakah dialog terdeteksi, dan untuk tiap label — ditemukan atau tidak, beserta tag/jenis/placeholder/potongan class kontrolnya. **Sengaja tidak memuat isi field, nama pegawai, atau data kegiatan**; ada test yang memastikan itu.
- **Bug yang nyaris lolos ke produksi**: `'\n'` di dalam template literal skrip dinormalkan oleh alat format menjadi baris baru sungguhan di tengah string, membuat seluruh bookmarklet gagal parse. Tertangkap karena test menjalankan `AUTOFILL_SCRIPT` yang sesungguhnya lewat `new Function` — kalau test hanya memeriksa fungsi TypeScript-nya, ini pasti lolos. Diperbaiki dengan membangun karakter barisnya saat jalan (`String.fromCharCode(10)`) sehingga tidak ada escape yang bisa dinormalkan.
- Diverifikasi: `npm run verify` bersih, 153 test (27 di berkas autofill), termasuk test baru untuk pencarian lewat placeholder, pencentangan lewat pembungkus, dan isi diagnosa.

[2026-08-18] Setelah tiga putaran perbaikan berbasis tangkapan layar tetap gagal, pengguna mengirim `outerHTML` halaman Pelaksanaan. **Itu mengubah segalanya: KipApp dibangun dengan Ant Design Vue 1.x** (`ant-select`, `ant-row`/`ant-col`, `ant-checkbox-wrapper`, `ant-calendar-picker`, `ant-table`; footer menyebut Versi 2.0.4). Selama ini pencocokan bersandar pada teks label dan kedekatan DOM — tebakan, padahal ada nama class kerangka yang stabil dan pasti. Lapisan pencarian kontrol ditulis ulang di atas nama class itu; penelusuran lewat teks label tetap disimpan sebagai cadangan.
- **Sebab kegagalan Tanggal akhirnya terjawab, dan bukan yang saya duga**: pemicu `ant-calendar-picker` dan `ant-time-picker` adalah input **readonly** — Ant Design memang hanya menerima nilai lewat popup-nya. Kode sebelumnya membuang setiap kontrol readonly, baik saat menelusuri label maupun placeholder, sehingga field itu **tidak akan pernah ditemukan sampai kapan pun**. Tiga putaran sebelumnya memperbaiki hal yang salah. Sekarang input readonly diterima sebagai pemicu, dan nilainya ditulis ke `ant-calendar-input` / `ant-time-picker-panel-input` di dalam popup.
- **Cacat serius yang tersingkap saat menulis ulang tiruannya**: kalau popup tidak terbuka, versi sebelumnya menulis langsung ke input pemicu yang readonly dan pemeriksaan nilai MENGIRA BERHASIL — padahal nilai itu tidak pernah sampai ke state komponen Vue, sehingga Save akan mengirim field kosong. Menulis ke pemicu readonly sekarang ditolak; popup yang tidak terbuka dilaporkan sebagai kegagalan, bukan dijadikan jalan cadangan.
- **Baris field**: Ant Design menaruh label dan kontrolnya di dua `ant-col` bersaudara dalam satu `ant-row`, jadi baris itulah wadah yang benar — menggantikan perambatan naik yang dulu bisa mengambil kontrol milik field lain.
- **Combobox**: pemicu `.ant-select-selection` diklik, kotak pencarian `input.ant-select-search__field` di dalamnya diisi, opsi `li.ant-select-dropdown-menu-item` (dipasang di `body`, bukan di dalam dialog) ditunggu lalu diklik, dan pemilihan diverifikasi lewat `.ant-select-selection-selected-value`.
- **Checkbox**: input aslinya tersembunyi di balik `.ant-checkbox-inner`, jadi lapisan itu yang diklik bila klik pada input tidak berpengaruh.
- **Tiruan test ditulis ulang memakai kerangka Ant Design yang sebenarnya**, disalin dari outerHTML asli — termasuk pemicu readonly, dropdown yang dipasang di `body`, dan checkbox berlapis. Tiruan lama memakai `<select>` dan `input type=date` biasa; **itulah sebabnya test hijau berkali-kali sementara form sungguhan gagal**. Pelajaran yang mahal: tiruan yang tidak setia bukan sekadar kurang berguna, ia menyesatkan.
- Diverifikasi: `npm run verify` bersih, 152 test (26 di berkas autofill). Yang belum terbukti tetap sama: perilaku di browser sungguhan.

[2026-08-18] Pengguna meminta autofill bisa sekali jalan untuk data sebulan, dengan bookmarklet yang membaginya jadi sesi dan tombol maju ke tanggal berikutnya. **Yang bisa diringkas bukan pengisiannya melainkan penyiapannya**: KipApp hanya menerima satu kegiatan per dialog (tekan Add, isi, Save, ulangi), jadi tidak ada cara mengisi banyak kegiatan dalam satu dialog tanpa mengotomatiskan tombol Save — yang justru batas yang sengaja dipegang (CON-03a). Karena itu bentuknya antrean: sekali salin untuk seluruh periode, lalu antreannya yang mengingat sudah sampai mana, bukan pengguna.
- **Payload jamak** (`kiplogAutofill: 2` dengan `items[]`) hidup berdampingan dengan payload tunggal versi 1 — kartu per kegiatan tetap berfungsi seperti sebelumnya, dan bookmarklet membedakan keduanya dari ada-tidaknya `items`.
- **Diurutkan menurut tanggal lalu jam, bukan per RK**, karena antreannya dikerjakan berurutan waktu di KipApp. Ini berbeda dari susunan halaman KipApp Ready yang berkelompok per RK, dan itu disengaja.
- **Kegiatan yang belum siap kirim dikeluarkan beserta alasannya** (sudah dikirim untuk dinilai, deskripsi/capaian kosong), tidak disertakan diam-diam: menemukan kegiatan yang pasti gagal di tengah antrean dua puluhan jauh lebih merugikan daripada tahu sejak awal berapa yang dilewati.
- **Posisi antrean disimpan di localStorage** (`kiplog-autofill-queue`, berkunci tanda tangan jumlah+tanggal pertama+tanggal terakhir) supaya selamat dari muat ulang halaman. Tanpa itu pengguna harus menghitung sendiri sudah sampai kegiatan ke berapa. Kunci itu juga mencegah posisi antrean lama terbawa ke antrean bulan lain.
- **Tombol "Lompat ke tanggal berikutnya"** melewati sisa kegiatan di hari yang sama — sesuai permintaan pengguna, karena kadang satu hari sudah selesai diinput sebagian lewat jalur lain.
- Panel TIDAK menandai sendiri kegiatan sebagai selesai: skrip tidak tahu apakah pengguna benar-benar menekan Save di KipApp, dan mengaku tahu hal yang tidak diketahui persis kesalahan yang sudah diperbaiki di putaran sebelumnya. Kemajuan digerakkan tombol Berikutnya.
- Diverifikasi: `npm run verify` bersih, 171 test (35 di berkas autofill), termasuk antrean berpindah kegiatan, melompat tanggal, melanjutkan setelah panel dimuat ulang, dan menolak antrean kosong. Belum diuji di KipApp sungguhan.

[2026-08-18] Pengguna meminta panel bookmarklet bisa dipindah dan diubah ukurannya. Ini bukan permintaan kosmetik: panel menempel di sudut kanan bawah, dan di situ pula tombol Save serta bagian bawah dialog KipApp berada — selama antrean sebulan dikerjakan, panel yang tidak bisa digeser justru menutupi bagian form yang sedang diperiksa.
- **Ubah ukuran memakai `resize: both` bawaan browser** (wajib disertai `overflow: auto`, tanpa itu `resize` tidak berlaku sama sekali) daripada menggambar sudut penarik sendiri — perilakunya sudah dikenal pengguna dan tidak ada kode tambahan yang bisa salah.
- **Geser lewat header**, dengan titik pegangan dipertahankan (panel bergeser sejauh kursor bergerak, bukan melompat supaya sudutnya menempel ke kursor). Penambat `right`/`bottom` dilepas saat geseran pertama, karena kalau tidak panel tertarik dari dua arah sekaligus.
- **Posisi dan ukuran diingat** di localStorage, karena antrean sebulan dikerjakan berkali-kali — memindahkannya sekali seharusnya cukup untuk seterusnya. Nilai yang disimpan diambil dari properti gaya yang kita setel sendiri, bukan hasil pengukuran tata letak: itulah angka yang persis diminta pengguna, tanpa pembulatan atau pengaruh transform induk. **Ini ketahuan dari test**, yang di jsdom (tanpa mesin tata letak) selalu membaca `offsetLeft` nol.
- **Posisi tersimpan dijepit ke dalam layar saat dipulihkan**: jendela bisa lebih kecil daripada saat posisi itu disimpan, dan panel yang dipulihkan di luar layar sama saja dengan panel yang hilang. Klik ganda pada header mengembalikannya ke sudut.
- Diverifikasi: `npm run verify` bersih, 178 test — termasuk geseran, penjepitan ke tepi layar, posisi yang bertahan setelah panel dijalankan ulang, klik ganda untuk reset, dan tombol tutup yang tidak ikut memicu geseran.

[2026-08-18] Pengguna meminta autofill dibuat otomatis penuh — mengisi, menekan Save, membuka Add, lanjut sendiri. Larangan CON-03a ("dilarang menekan Save/Submit") dibacakan kembali beserta kasus terburuknya (RK salah terpilih diam-diam lalu puluhan kegiatan tercatat di RK yang keliru, di sistem BPS, dan yang sudah dikirim untuk dinilai tidak bisa diubah lagi); pengguna memilih otomatis penuh. Dikerjakan, CON-03a direvisi terbuka, dan CON-03b ditambahkan sebagai syarat yang mengikat mode itu.
- **Berhasil diukur dari MENUTUPNYA DIALOG, bukan dari notifikasi.** Ini menghapus kebutuhan meminta markup notifikasi ke pengguna: setelah Save diterima KipApp dialognya menutup, kalau validasi menolak dialognya tetap terbuka. Sinyal itu melekat pada perilaku aplikasinya sendiri, bukan pada teks atau nama class yang bisa berganti — pelajaran dari empat putaran sebelumnya.
- **Berhenti total pada kegagalan pertama**, tidak pernah melewati: pengisian tidak lengkap, simpanan ditolak, dialog tidak menutup, atau tombol Add tidak ada. Melanjutkan setelah satu kegagalan adalah cara tercepat mengisi sistem resmi dengan data salah tanpa ada yang menyadarinya.
- **Penanda anti-ganda** per kegiatan (tanggal + deskripsi, berkunci antrean) supaya menjalankan ulang panel di tengah antrean tidak menyimpan ulang yang sudah masuk — menghapus entri ganda di KipApp harus satu per satu.
- **Lingkup dialog dihitung ulang tiap iterasi**, tidak sekali di awal: dalam mode otomatis dialog dibuat dan dimusnahkan berkali-kali, dan rujukan ke dialog yang sudah dibuang membuat seluruh pencarian field menunjuk ke ruang kosong.
- **Cacat tiruan yang ketahuan saat test dijalankan**: `renderFakeKipAppDialog` menimpa seluruh `document.body`, sehingga saat mesin otomatis menekan Add, panel bookmarklet ikut terhapus dan dua test gagal — padahal kodenya benar. KipApp adalah SPA yang hanya merender ulang bagiannya sendiri. Tiruan diperbaiki agar merender ke wadahnya sendiri. **Ini kali kedua tiruan yang tidak setia menyesatkan** (yang pertama: `<select>` dan `input type=date` alih-alih komponen Ant Design).
- **Catatan proses**: perintah test sempat diblokir pengaman sesi ini sehingga kode ini sempat ditulis tanpa bisa diverifikasi; pengguna menambahkan aturan izin di `.claude/settings.json` (folder itu ter-gitignore, jadi lokal saja) dan test bisa dijalankan lagi. Dua kegagalan di atas langsung tertangkap begitu bisa dijalankan — bukti bahwa menahan diri untuk tidak mengaku berhasil tanpa menjalankan test itu tepat.
- Diverifikasi: `npm run verify` bersih, 184 test. Enam test khusus mode otomatis: menyimpan seluruh antrean sendiri, berhenti saat pengisian tidak lengkap, berhenti saat KipApp menolak (dan tidak menganggapnya tersimpan), tidak menyimpan ulang saat dijalankan lagi, membuka Add sendiri, dan berhenti kalau tombol Add tidak ada. **Belum diuji di KipApp sungguhan** — dan prasyaratnya tetap: mode manual harus terbukti bersih dulu.

[2026-08-18] **Mode otomatis dikonfirmasi BERJALAN BAIK di KipApp sungguhan oleh pengguna.** Ini konfirmasi pertama bahwa seluruh rantai Ant Design (combobox RK yang harus diklik opsinya, pemilih tanggal readonly yang hanya menerima nilai lewat popup, checkbox berlapis, deteksi berhasil dari menutupnya dialog) memang benar di luar tiruan test. Peringatan "belum diuji di KipApp sungguhan" pada entri-entri sebelumnya karena itu sudah terjawab untuk jalur utamanya.
- Pengguna lalu meminta pilihan jumlah kegiatan sekali jalan (10/20/30/40/semua). Ditambahkan sebagai `select` di panel antrean, pilihannya diingat di localStorage. Alasannya sejalan dengan pengaman yang sudah ada: antrean sebulan bisa dua puluhan entri, dan menjalankan semuanya sekaligus berarti dua puluhan simpanan ke sistem resmi sebelum ada kesempatan memeriksa hasilnya.
- **Bawaannya tetap "semua"** supaya perilaku yang baru saja pengguna buktikan bekerja tidak berubah diam-diam; pembatasan adalah pilihan sadar, bukan kejutan.
- **Batas diperiksa SESUDAH maju ke kegiatan berikutnya**, bukan sebelumnya, supaya panel berhenti sambil menunjuk kegiatan yang belum dikerjakan. Kalau ia berhenti sambil menunjuk kegiatan yang barusan selesai, menekan lanjut akan menyimpannya dua kali — ada test khusus untuk ini.
- Jeda karena batas tercapai dibedakan dari selesai maupun gagal: pesannya menyebut berapa yang tersimpan, berapa sisanya, dan menyarankan memeriksa daftar Pelaksanaan sebelum melanjutkan.
- Diverifikasi: `npm run verify` bersih, 189 test (53 di berkas autofill), termasuk berhenti tepat pada jumlah yang dipilih, melanjutkan sisa tanpa mengulang, menjalankan semua saat dipilih "semua", posisi berhenti yang benar, dan pilihan jumlah yang diingat.

[2026-08-18] Pengguna melaporkan antrean otomatis berhenti di kegiatan ke-5 dengan pesan "pengisian tidak lengkap"; tangkapan layarnya memperlihatkan kalender TERBUKA, `2026-05-05` sudah terketik di kotak isiannya, dan tanggal 5 tersorot di grid — tetapi nilainya tidak pernah masuk ke field. Jadi pengamannya bekerja persis sebagaimana dirancang (berhenti pada kegagalan pertama, tidak melanjutkan), dan yang salah adalah cara mengunci tanggalnya.
- **Sebabnya: Ant Design v1 memeriksa `event.keyCode`, bukan `event.key`** — dan `keyCode` TIDAK terisi dari opsi konstruktor `KeyboardEvent`, jadi Enter yang dikirim skrip selalu terbaca sebagai tombol kosong. Sekarang `keyCode`/`which` dipasang lewat `Object.defineProperty` pada event-nya, dan urutan `keydown`+`keypress`+`keyup` dikirim lengkap.
- **Ditambah jalan kedua: mengklik tanggalnya di grid kalender**, yang memang dilakukan manusia dan paling dipercaya komponennya. Mengetik ke kotak isian sudah membuat panel berpindah ke bulan yang benar dan menyorot tanggalnya (terlihat di tangkapan layar), jadi tinggal menekan selnya. Sel bulan sebelumnya/berikutnya yang ikut tampil di tepi grid sengaja dilewati supaya tidak salah memilih tanggal berangka sama.
- **Popup ditutup (Escape) sebelum melaporkan gagal**: kalender yang dibiarkan terbuka menghalangi field berikutnya, dan itu melanggar prinsip "tidak meninggalkan form lebih buruk daripada saat ditemukan".
- **Kenapa ini tidak tertangkap test sebelumnya**: tiruan kalender di test menerima Enter dari `event.key` saja, sehingga jalur yang gagal di produksi justru mulus di test. **Ini kali ketiga tiruan yang tidak setia menyesatkan.** Tiruannya kini punya dua varian yang masing-masing mengunci satu jalur: satu yang HANYA menerima Enter ber-keyCode, satu lagi yang mengabaikan Enter sepenuhnya sehingga hanya klik grid yang bisa mengunci — lengkap dengan sel jebakan berangka sama dari bulan lain.
- Diverifikasi: `npm run verify` bersih, 193 test (57 di berkas autofill).

[2026-08-18] Pengguna melaporkan antrean otomatis berhenti lagi di kegiatan berikutnya dengan gejala yang sama persis (kalender terbuka, tanggal terketik, tersorot di grid, tapi tidak masuk), dan panelnya "stuck" sampai tombol Jalankan otomatis ditekan lagi. Tangkapan layarnya memuat satu petunjuk yang menentukan: **popup kalendernya MASIH TERBUKA**, padahal versi yang baru saja dipush menutupnya (Escape) sebelum melaporkan gagal. Artinya besar kemungkinan bookmark yang dipakai masih versi lama — skrip tertanam di dalam bookmark dan TIDAK ikut ter-update saat aplikasi di-deploy.
- **Ambiguitas itu sendiri yang diperbaiki lebih dulu**: ditambahkan `AUTOFILL_VERSION` yang tampil di judul panel ("KipLog autofill v8") dan disebut juga di kartu pemasangan. Menebak versi bookmark dari gejalanya sudah menghabiskan satu putaran; sekarang cukup dilihat di tangkapan layar.
- **Verifikasi tanggal dilonggarkan, dan ini kemungkinan sebab sesungguhnya**: syaratnya semula "nilai field SAMA PERSIS dengan yang diketik". Pemilih tanggal Ant Design bisa menampilkan kembali nilainya dalam format lain daripada yang dimasukkan, sehingga tanggal yang SUDAH masuk tetap dilaporkan gagal. Sekarang diterima bila nilainya sama persis, ATAU berubah menjadi tidak kosong — field itu kosong sebelum diisi dan tiap dialog selalu baru, jadi "berubah dan tidak kosong" sudah menentukan. Test baru menjaga dua sisinya: format lain diterima, tidak berubah sama sekali tetap gagal.
- **Klik sel kalender diperluas**: setelah mengklik isi selnya, selnya sendiri ikut diklik, karena sebagian versi memasang penanganya di `<td>` bukan di isinya.
- **Tombol "Lewati kegiatan ini & lanjutkan otomatis"** muncul saat mode otomatis berhenti karena gagal. Berhenti pada kegagalan pertama tetap benar, tapi memaksa pengguna menekan Jalankan otomatis lagi setelah memperbaiki manual membuat antrean 38 kegiatan terasa macet. Melewati tetap tindakan SADAR pengguna, bukan otomatis — kegiatan yang dilewati tidak ditandai tersimpan, jadi tetap muncul lagi kalau antrean diulang.
- Diverifikasi: `npm run verify` bersih, 198 test (62 di berkas autofill).

[2026-08-18] Percobaan berikutnya pengguna membawa dua kabar. **Perbaikan tanggal berhasil** — tangkapan layarnya memperlihatkan field Tanggal terisi `2026-05-05`, jadi jalur keyCode + klik grid memang benar. Tetapi antrean berhenti dengan pesan "Masukan ke capaian SKP (centang tidak berubah)" **padahal di layar centangnya jelas sudah berubah**.
- **Sebabnya: Vue merender ulang secara asinkron**, sedangkan `setChecked` membaca keadaan centang SEKETIKA setelah klik. Bacaan basi itu disimpulkan sebagai gagal, lalu dicoba mengklik lapisan berikutnya — membolak-balik centang yang sebenarnya sudah benar — dan akhirnya melaporkan gagal untuk field yang di layar tercentang. Ini kesalahan yang sama bentuknya dengan "mengaku tanpa memeriksa", hanya terbalik: memeriksa terlalu cepat lalu menyimpulkan yang salah.
- Diganti `setCheckedAsync` yang MENUNGGU tiap percobaan sebelum menilai, dan hanya menaikkan ke cara berikutnya kalau yang sebelumnya benar-benar tidak berpengaruh. Keadaan centang juga dibaca dari dua sumber: properti `checked` input DAN kelas `ant-checkbox-checked` pada pembungkusnya — kelas itu lebih dulu mencerminkan keadaan baru.
- **Escape ternyata tidak menutup kalender KipApp**; yang menutup adalah klik di tempat lain pada dialog (dikonfirmasi pengguna). `dismissPopup()` sekarang mengklik judul dialog, dipakai (a) sesudah tanggal terkunci, (b) sebagai percobaan TERAKHIR mengunci nilai sebelum menyerah — klik luar lazim membuat komponen mengunci nilai yang sudah diketik — dan (c) sebelum menekan Save di mode otomatis, karena kalender yang menggantung terbuka menutupi tombol Save dan akan membuat antrean berhenti bukan karena isinya salah melainkan karena tombolnya tidak terjangkau.
- **Verifikasi tanggal dilonggarkan**: diterima bila nilainya sama persis, ATAU berubah menjadi tidak kosong. Pemilih tanggal boleh menampilkan kembali nilainya dalam format lain, dan menolaknya berarti melaporkan gagal untuk tanggal yang sudah masuk.
- **Penanda versi skrip** (`AUTOFILL_VERSION`, tampil di judul panel) ditambahkan setelah satu putaran terbuang karena tidak bisa memastikan bookmark pengguna sudah versi terbaru atau belum — skrip tertanam di bookmark dan tidak ikut ter-update saat deploy.
- **Tombol "Lewati kegiatan ini & lanjutkan otomatis"** muncul saat mode otomatis berhenti, supaya satu kegiatan bermasalah tidak memaksa seluruh antrean 38 kegiatan diulang. Melewati tetap tindakan sadar pengguna dan TIDAK menandai kegiatan itu tersimpan.
- Diverifikasi: `npm run verify` bersih, 204 test (67 di berkas autofill).

[2026-08-18] Pengguna melaporkan gejala baru pada v10: panel berjalan dan progresnya maju, tetapi form KipApp tidak tersentuh sama sekali dan tidak ada data yang masuk. Penyebabnya penanda anti-ganda: kegiatan-kegiatan itu tertandai "sudah pernah disimpan" dari percobaan sebelumnya, lalu dilewati cepat-cepat tanpa penjelasan yang sempat terbaca. Pengguna kemudian memberi keterangan yang menyelesaikan perdebatannya: **ia bisa menghapus entri langsung di KipApp**, jadi penanda itu pasti salah cepat atau lambat.
- **Seluruh mesin penanda dicabut** — `SAVED_KEY`, `markSaved`, `alreadySaved`, daftar tanda tangan per kegiatan, dan cabang melewatinya. Penanda semacam itu adalah tebakan LOKAL tentang keadaan sistem lain; begitu tebakannya meleset, akibatnya justru yang paling berbahaya: kegiatan yang belum masuk dilewati diam-diam sementara panel terlihat bekerja. Lebih baik antrean selalu mengerjakan kegiatan yang sedang ditunjuknya dan pengguna yang menentukan mulai dari mana.
- **Yang tetap ada**: posisi antrean di localStorage. Itu catatan tentang panel sendiri ("sedang menunjuk kegiatan ke berapa"), bukan klaim tentang isi KipApp, jadi ia tidak bisa menjadi salah dengan cara yang sama.
- **Konsekuensi yang disadari dan diterima**: menjalankan ulang antrean atas kegiatan yang sudah masuk akan memasukkannya lagi. Perlindungan terhadap itu sekarang ada di tangan pengguna — posisi antrean, pilihan jumlah sekali jalan, dan memeriksa daftar Pelaksanaan. CON-03b direvisi: syarat "menyimpan penanda anti-ganda" diganti larangan menyimpannya, dengan alasannya dicatat.
- **Pelajaran yang berulang** sepanjang fitur ini: setiap kali skrip menyimpan kesimpulan tentang keadaan di luar dirinya — "sudah tersimpan", "berhasil karena notifikasi muncul", "gagal karena bacaan seketika" — kesimpulan itu jadi sumber kesalahan berikutnya. Yang bertahan hanyalah pengamatan langsung pada saat itu juga: dialog menutup atau tidak, nilai berubah atau tidak.
- Diverifikasi: `npm run verify` bersih, 203 test (67 di berkas autofill).

[2026-08-18] Pengguna meminta unduhan di menu Laporan berupa ZIP berisi satu PDF per kegiatan, dengan nama berformat `YYYYMMDD_Bukti Dukung Kegiatan_Fadly Muhamad Akbar` dan bisa dikustom.
- **PDF-nya dibuat lewat `generateDataDukungPdf` yang sama** dengan laporan gabungan, hanya diberi satu kegiatan per panggilan. Menulis ulang tata letaknya untuk jalur baru ini akan membuat dua versi dokumen resmi yang bisa berbeda diam-diam.
- **Dibuat berurutan, bukan paralel**: tiap PDF mendekode gambar bukti dukung di memori, dan mengerjakan puluhan sekaligus pada perangkat biasa berisiko kehabisan memori di tengah jalan. Kemajuannya ditampilkan di tombol ("Membuat ZIP… 7/38") karena proses ini memang lama.
- **Pola nama memakai token** (`{tanggal}`, `{tanggal-strip}`, `{nama}`, `{nip}`, `{kegiatan}`, `{rk}`) dengan bawaan persis seperti yang diminta. Token yang salah ketik DIBIARKAN apa adanya di nama berkas, tidak dihapus: token yang lenyap tanpa jejak membuat pengguna mengira polanya sudah benar. Contoh hasil ditampilkan langsung di bawah kolom isian supaya polanya bisa dinilai sebelum ZIP dibuat.
- **Nama kembar diberi nomor urut**, dan ini bukan kehalusan: pola bawaan hanya memuat tanggal dan nama pegawai, sehingga dua kegiatan pada hari yang sama PASTI bernama sama — ZIP dengan dua entri identik akan kehilangan salah satunya saat diekstrak. Perbandingannya mengabaikan besar-kecil huruf karena Windows juga begitu.
- **Sanitasi nama** mengganti `\ / : * ? " < > |` dan membuang karakter kontrol serta titik/spasi di ujung. Garis miring bukan kasus langka di sini — deskripsi kegiatan pengguna memuat "Survei/ Sensus".
- **Pola disimpan di localStorage, bukan di AppSettings**: ini preferensi pengunduhan di satu perangkat, bukan data kerja yang perlu ikut tersinkron ke Drive.
- Diverifikasi: `npm run verify` bersih, 215 test (12 baru untuk penamaan). Yang BELUM diverifikasi: isi ZIP-nya di browser sungguhan — aplikasi terkunci di layar login Google, jadi pembuatan PDF end-to-end perlu dicoba pengguna.

[2026-08-18] Pengguna meminta tampilan diperindah, disertai fitur foto profil, gambar-gambar, menu logo aplikasi, dan animasi saat masuk dashboard/menggulir/pindah menu.
- **Dua field baru, ditaruh di tempat yang berbeda dengan sengaja**: `photoDataUrl` di UserProfile (identitas orangnya) dan `appLogoDataUrl` di AppSettings (tampilan aplikasinya). Keduanya `optional()` sehingga data lama tetap sah tanpa migrasi Dexie, mengikuti pola `accentColor` sebelumnya.
- **Gap yang sekalian tertutup**: `logoDataUrl` sudah dipakai kop PDF sejak Fase 5 tetapi TIDAK PERNAH punya UI untuk mengunggahnya — entri ASSUMPTIONS lama menyebut "pengguna dapat mengunggah via My Profile", padahal jalannya tidak ada. Sekarang ada.
- **Gambar diciutkan ke maksimal 256px sebelum disimpan.** Foto dan logo hanya pernah dirender 32–96px; menyimpan berkas kamera 4 MB sebagai data URL membebani IndexedDB DAN setiap sinkronisasi Drive, karena ikut ke `kiplog-data.json`. PNG dipertahankan untuk logo bertransparansi, sisanya jadi JPEG.
- **Animasi hanya menganimasikan `opacity` dan `transform`** — dua properti yang bisa dikerjakan compositor tanpa menghitung ulang tata letak. Menganimasikan tinggi atau posisi akan membuat halaman padat data seperti Kalender tersendat di perangkat kelas menengah.
- **Animasi pindah menu memakai `key={location.pathname}`** pada pembungkus konten, supaya React memasang ulang bloknya dan animasinya terputar lagi. Tanpa itu animasi hanya terlihat sekali seumur sesi.
- **`Reveal` memakai IntersectionObserver, bukan pendengar `scroll`**: pendengar scroll berjalan puluhan kali per detik dan memaksa pembacaan tata letak, yang justru membuat gulirannya tersendat — kebalikan dari tujuannya. Sekali muncul tidak pernah disembunyikan lagi, karena elemen yang memudar tiap kali digulir bolak-balik terasa seperti kerusakan. Isinya tetap ada di DOM saat belum tampak supaya pembaca layar dan pencarian dalam halaman tidak kehilangan apa pun.
- **Guard `prefers-reduced-motion` yang sudah ada sejak polesan Agustus 2026 tetap dihormati**: pengguna yang mematikan animasi di sistemnya mendapat NOL gerakan, bukan gerakan yang dipercepat.
- Diverifikasi: `npm run verify` bersih, 219 test (4 baru untuk `Reveal`). CSS bundle naik 36,18 KB → 37,40 KB (gzip 7,17 → 7,40 KB). **Belum dilihat langsung di browser** — aplikasi terkunci di layar login Google, jadi hasil visualnya perlu dinilai pengguna.
